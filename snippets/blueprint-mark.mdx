{/* XDS blueprint mark — ambient dashed-line illustrations extracted
    from xds.pages.twitter.biz/visuals (self-animated SVGs in
    /images/visuals: conversations, audiences, broadcast, attention,
    targeting, classification). Renders inside an x-surface banner;
    the dash-flow loop pauses under prefers-reduced-motion. */}
export const BlueprintMark = ({ name, height = 150 }) => (
  <div
    className="not-prose x-surface"
    style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '28px 0',
      margin: '4px 0 24px',
      overflow: 'hidden'
    }}
  >
    <img
      src={`/images/visuals/${name}.svg`}
      alt=""
      aria-hidden="true"
      style={{ height: `${height}px`, width: 'auto' }}
    />
  </div>
);
