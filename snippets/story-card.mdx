{/* Story card on an XDS surface: 3:2 image well over a headline2
    title. The image well stays white so source imagery reads true. */}
export const StoryCard = ({ img, href, title }) => {
  return (
    <a href={href} className="x-surface block not-prose font-normal group relative my-2 overflow-hidden w-full cursor-pointer">
      <div className="flex flex-col h-full">
        <div className="w-full aspect-[3/2] bg-white">
          <img
            src={img}
            className="rounded-none pointer-events-none w-full h-full object-cover"
            alt={title}
          />
        </div>
        <div className="flex flex-col justify-center p-4 flex-1" style={{ background: 'var(--x-tile-bg)' }}>
          <div className="x-headline2 x-text-primary">{title}</div>
        </div>
      </div>
    </a>
  );
};
