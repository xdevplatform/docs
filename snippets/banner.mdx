{/* Partner banner on an XDS surface (.x-surface): border-normal +
    shadow-sm at rest, border-hover + hover-subtle wash on hover.
    Logo well stays white in both themes so partner marks read true. */}
export const Banner = ({ img, href, title, description }) => {
  return (
    <a href={href} className="x-surface block not-prose font-normal group relative my-1 overflow-hidden w-full cursor-pointer">
      <div className="flex flex-row h-full">
        <div className="integration-logo w-48 flex items-center justify-center p-4 bg-white">
          <span className="flex items-center justify-center" style={{ width: '100%', height: '100%', maxWidth: '150px' }}>
            <img
              src={img}
              className="pointer-events-none w-full h-full object-contain"
              style={{ maxWidth: '150px', maxHeight: '80px' }}
            />
          </span>
        </div>
        <div className="flex flex-col justify-center p-4 flex-1">
          <h2 className="x-headline2 x-text-primary">{title}</h2>
          <p className="mt-1 x-subtext2 x-text-secondary">{description}</p>
        </div>
      </div>
    </a>
  );
};
