{/* Partner logo card on an XDS surface. The logo well keeps its
    configurable background (default white) so partner marks render
    on the surface they were designed for. */}
export const LogoCard = ({ img, href, title, description, backgroundColor = 'white' }) => {
  return (
    <a
      href={href}
      className="x-surface block not-prose font-normal group relative my-2 overflow-hidden w-full cursor-pointer"
    >
      <div className="flex flex-col h-full">
        <div
          className="integration-logo w-full h-32 flex items-center justify-center overflow-hidden"
          style={{ backgroundColor }}
        >
          <img
            src={img}
            alt={`${title} logo`}
            className="w-full h-full object-contain px-4 py-2"
          />
        </div>
        <div className="p-4 flex-grow">
          <h3 className="x-headline2 x-text-primary mb-2">{title}</h3>
          <p className="x-subtext1 x-text-secondary">{description}</p>
        </div>
      </div>
    </a>
  );
};
