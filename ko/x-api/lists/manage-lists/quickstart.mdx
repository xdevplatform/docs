---
title: 퀵스타트
sidebarTitle: 퀵스타트
description: "이 가이드는 List를 생성, 업데이트, 삭제하는 방법을 안내합니다. Manage lists를 다루는 X API v2 standard 티어 레퍼런스입니다."
keywords: ["manage lists 퀵스타트", "list 생성 튜토리얼", "list 관리 가이드"]
---

import { Button } from '/snippets/button.mdx';

이 가이드는 List를 생성, 업데이트, 삭제하는 방법을 안내합니다.

<Note>
**사전 요구사항**

시작하기 전에 다음이 필요합니다:
- 승인된 App이 포함된 [developer account](https://developer.x.com/en/portal/petition/essential/basic-info)
- User Access Token (OAuth 1.0a 또는 OAuth 2.0 PKCE)
</Note>

---

## List 생성

<Steps>
  <Step title="요청 준비">
    List 이름(필수)과 선택적 설명 및 비공개 설정을 정의합니다:

    ```json
    {
      "name": "Tech News",
      "description": "Top tech journalists and publications",
      "private": false
    }
    ```
  </Step>

  <Step title="요청 보내기">

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/lists" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Tech News",
    "description": "Top tech journalists and publications",
    "private": false
  }'
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# 새 List 생성
response = client.lists.create(
    name="Tech News",
    description="Top tech journalists and publications",
    private=False
)

print(f"List created: {response.data.id} - {response.data.name}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// 새 List 생성
const response = await client.lists.create({
  name: "Tech News",
  description: "Top tech journalists and publications",
  private: false,
});

console.log(`List created: ${response.data?.id} - ${response.data?.name}`);
```

</CodeGroup>

  </Step>

  <Step title="응답 확인">
    ```json
    {
      "data": {
        "id": "1441162269824405510",
        "name": "Tech News"
      }
    }
    ```

    나중에 List를 업데이트하거나 삭제하려면 `id`를 저장하세요.
  </Step>
</Steps>

---

## List 업데이트

List의 이름, 설명 또는 비공개 여부 수정:

<CodeGroup dropdown>

```bash cURL
curl -X PUT "https://api.x.com/2/lists/1441162269824405510" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Tech News & Insights",
    "description": "Updated description"
  }'
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# List 업데이트
response = client.lists.update(
    "1441162269824405510",
    name="Tech News & Insights",
    description="Updated description"
)

print(f"Updated: {response.data.updated}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// List 업데이트
const response = await client.lists.update("1441162269824405510", {
  name: "Tech News & Insights",
  description: "Updated description",
});

console.log(`Updated: ${response.data?.updated}`);
```

</CodeGroup>

**응답:**

```json
{
  "data": {
    "updated": true
  }
}
```

---

## List 삭제

<Steps>
  <Step title="List ID 가져오기">
    삭제하려는 List의 ID가 필요합니다.
  </Step>

  <Step title="삭제 요청 보내기">

<CodeGroup dropdown>

```bash cURL
curl -X DELETE "https://api.x.com/2/lists/1441162269824405510" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# List 삭제
response = client.lists.delete("1441162269824405510")
print(f"Deleted: {response.data.deleted}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// List 삭제
const response = await client.lists.delete("1441162269824405510");
console.log(`Deleted: ${response.data?.deleted}`);
```

</CodeGroup>

  </Step>

  <Step title="삭제 확인">
    ```json
    {
      "data": {
        "deleted": true
      }
    }
    ```
  </Step>
</Steps>

<Warning>
소유한 List만 삭제할 수 있습니다.
</Warning>

---

## 다음 단계

<CardGroup cols={2}>
  <Card title="List 멤버" icon="/icons/xds/icon-people.svg" href="/x-api/lists/list-members/introduction">
    List 멤버 추가 및 제거
  </Card>
  <Card title="List lookup" icon="/icons/xds/icon-search.svg" href="/x-api/lists/list-lookup/quickstart">
    List 세부 정보 조회
  </Card>
  <Card title="통합 가이드" icon="/icons/xds/icon-book.svg" href="/x-api/lists/manage-lists/integrate">
    핵심 개념 및 모범 사례
  </Card>
  <Card title="API 레퍼런스" icon="/icons/xds/icon-code.svg" href="/x-api/lists/create-list">
    전체 엔드포인트 문서
  </Card>
</CardGroup>
