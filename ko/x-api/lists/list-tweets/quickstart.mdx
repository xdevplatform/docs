---
title: 퀵스타트
sidebarTitle: 퀵스타트
description: "이 가이드는 List 타임라인에서 Post를 조회하는 방법을 안내합니다. List tweets를 다루는 X API v2 standard 티어 레퍼런스입니다."
keywords: ["list tweets 퀵스타트", "list 타임라인 퀵스타트", "list post 가져오기 튜토리얼"]
---

import { Button } from '/snippets/button.mdx';

이 가이드는 List 타임라인에서 Post를 조회하는 방법을 안내합니다.

<Note>
**사전 요구사항**

시작하기 전에 다음이 필요합니다:
- 승인된 App이 포함된 [developer account](https://developer.x.com/en/portal/petition/essential/basic-info)
- App의 Bearer Token
</Note>

---

<Steps>
  <Step title="List ID 찾기" icon="/icons/xds/icon-bulleted-list.svg">
    x.com에서 List를 볼 때 URL에서 List ID를 찾을 수 있습니다:

    ```
    https://x.com/i/lists/84839422
                          └── 이 부분이 List ID
    ```
  </Step>

  <Step title="List 타임라인 요청" icon="terminal">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/84839422/tweets?\
tweet.fields=created_at,public_metrics,author_id&\
expansions=author_id&\
user.fields=username,verified&\
max_results=10" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# 페이지네이션으로 List에서 Post 가져오기
for page in client.lists.get_tweets(
    "84839422",
    tweet_fields=["created_at", "public_metrics", "author_id"],
    expansions=["author_id"],
    user_fields=["username", "verified"],
    max_results=10
):
    for post in page.data:
        print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// 페이지네이션으로 List에서 Post 가져오기
const paginator = client.lists.getTweets("84839422", {
  tweetFields: ["created_at", "public_metrics", "author_id"],
  expansions: ["author_id"],
  userFields: ["username", "verified"],
  maxResults: 10,
});

for await (const page of paginator) {
  page.data?.forEach((post) => {
    console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
  });
}
```

</CodeGroup>

  </Step>

  <Step title="응답 확인" icon="eye">
    ```json
    {
      "data": [
        {
          "id": "1458172421115101189",
          "text": "Check out our latest announcement...",
          "author_id": "4172587277",
          "created_at": "2024-01-15T10:30:00.000Z",
          "public_metrics": {
            "retweet_count": 42,
            "reply_count": 5,
            "like_count": 156,
            "quote_count": 3
          },
          "edit_history_tweet_ids": ["1458172421115101189"]
        }
      ],
      "includes": {
        "users": [
          {
            "id": "4172587277",
            "username": "TechNews",
            "verified": true
          }
        ]
      },
      "meta": {
        "result_count": 1,
        "next_token": "7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix"
      }
    }
    ```
  </Step>

  <Step title="결과 페이지네이션" icon="/icons/xds/icon-arrow-right.svg">
    SDK는 페이지네이션을 자동으로 처리합니다. cURL의 경우 응답에서 `next_token`을 사용하여 더 많은 Post를 가져옵니다:

    ```bash
    curl "https://api.x.com/2/lists/84839422/tweets?\
    max_results=10&\
    pagination_token=7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Step>
</Steps>

<Note>
이 엔드포인트는 List의 가장 최근 Post를 최대 800개까지 반환합니다.
</Note>

---

## 다음 단계

<CardGroup cols={2}>
  <Card title="List lookup" icon="/icons/xds/icon-bulleted-list.svg" href="/x-api/lists/list-lookup/quickstart">
    List 세부 정보 가져오기
  </Card>
  <Card title="List 멤버" icon="/icons/xds/icon-people.svg" href="/x-api/lists/list-members/introduction">
    List 멤버 가져오기
  </Card>
  <Card title="통합 가이드" icon="/icons/xds/icon-book.svg" href="/x-api/lists/list-tweets/integrate">
    핵심 개념 및 모범 사례
  </Card>
  <Card title="API 레퍼런스" icon="/icons/xds/icon-code.svg" href="/x-api/lists/get-list-posts">
    전체 엔드포인트 문서
  </Card>
</CardGroup>
