---
title: Manage Mutes
sidebarTitle: Manage Mutes
description: "이 가이드는 X API를 사용하여 사용자를 뮤트하거나 뮤트 해제하는 방법을 안내합니다. 퀵스타트를 다루는 X API v2 standard 티어 레퍼런스입니다."
keywords: ["manage mutes 퀵스타트", "사용자 뮤트 퀵스타트", "manage mutes 튜토리얼", "mute 가이드", "mutes 퀵스타트"]
---

import { Button } from '/snippets/button.mdx';

이 가이드는 X API를 사용하여 사용자를 뮤트하거나 뮤트 해제하는 방법을 안내합니다.

<Note>
**사전 요구사항**

시작하기 전에 다음이 필요합니다:
- 승인된 App이 포함된 [developer account](https://developer.x.com/en/portal/petition/essential/basic-info)
- User Access Token (OAuth 1.0a 또는 OAuth 2.0 PKCE)
</Note>

---

## 사용자 뮤트

<Steps>
  <Step title="사용자 ID 가져오기">
    인증된 사용자의 ID가 필요합니다. [user lookup 엔드포인트](/x-api/users/lookup/introduction)를 사용하거나 Access Token(숫자 부분이 사용자 ID)에서 찾을 수 있습니다.
  </Step>

  <Step title="대상 사용자 ID 가져오기">
    [user lookup 엔드포인트](/x-api/users/lookup/introduction)를 사용하여 뮤트하려는 계정의 사용자 ID를 찾습니다.
  </Step>

  <Step title="뮤트 요청 보내기">

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/users/123456789/muting" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"target_user_id": "9876543210"}'
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# 사용자 뮤트
response = client.users.mute(
    source_user_id="123456789",
    target_user_id="9876543210"
)

print(f"Muting: {response.data.muting}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// 사용자 뮤트
const response = await client.users.mute("123456789", {
  targetUserId: "9876543210",
});

console.log(`Muting: ${response.data?.muting}`);
```

</CodeGroup>

  </Step>

  <Step title="응답 확인">
    ```json
    {
      "data": {
        "muting": true
      }
    }
    ```
  </Step>
</Steps>

---

## 사용자 뮤트 해제

사용자에 대한 뮤트를 제거합니다:

<CodeGroup dropdown>

```bash cURL
curl -X DELETE "https://api.x.com/2/users/123456789/muting/9876543210" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# 사용자 뮤트 해제
response = client.users.unmute(
    source_user_id="123456789",
    target_user_id="9876543210"
)

print(f"Muting: {response.data.muting}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// 사용자 뮤트 해제
const response = await client.users.unmute("123456789", "9876543210");

console.log(`Muting: ${response.data?.muting}`);
```

</CodeGroup>

**응답:**

```json
{
  "data": {
    "muting": false
  }
}
```

---

## 뮤트 vs 차단

| Feature | Mute | Block |
|:--------|:-----|:------|
| 상대방의 Post 보기 | No | No |
| 상대방이 내 Post 보기 | Yes | No |
| 상대방이 나를 팔로우 | Yes | No |
| 상대방이 나에게 DM 가능 | Yes | No |
| 상대방이 알 수 있음 | No | Yes |

---

## 다음 단계

<CardGroup cols={2}>
  <Card title="Mutes lookup" icon="volume-xmark" href="/x-api/users/mutes/quickstart/mutes-lookup">
    뮤트된 사용자 가져오기
  </Card>
  <Card title="Blocks" icon="/icons/xds/icon-block.svg" href="/x-api/users/blocks/quickstart">
    대신 사용자 차단하기
  </Card>
  <Card title="API 레퍼런스" icon="/icons/xds/icon-code.svg" href="/x-api/users/mute-user-by-user-id">
    전체 엔드포인트 문서
  </Card>
</CardGroup>
