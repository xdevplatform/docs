---
title: "StreamClient"
sidebarTitle: "StreamClient"
description: "X API TypeScript SDK의 StreamClient 클래스에 대한 레퍼런스. X API v2 stream 엔드포인트에 대한 메서드, 매개변수 및 반환 타입."
---

## 생성자

### constructor

• **new StreamClient**(`client`): [`StreamClient`](/xdks/typescript/reference/classes/StreamClient)

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `client` | [`Client`](/xdks/typescript/reference/classes/Client) |

#### 반환값

[`StreamClient`](/xdks/typescript/reference/classes/StreamClient)

#### 정의 위치

[stream/stream_client.ts:972](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L972)

## 메서드

### likesCompliance

▸ **likesCompliance**(`options?`): `Promise`\<`EventDrivenStream`\>

Stream Likes compliance data
사용자의 Likes와 관련된 모든 compliance 데이터를 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `options` | `LikesComplianceStreamingOptions` |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:1016](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L1016)

___

### usersCompliance

▸ **usersCompliance**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream Users compliance data
사용자와 관련된 모든 compliance 데이터를 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `UsersComplianceStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:1196](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L1196)

___

### postsFirehoseKo

▸ **postsFirehoseKo**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream Korean Posts
모든 공개 한국어 Posts를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsFirehoseKoStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:1392](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L1392)

___

### likesSample10

▸ **likesSample10**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream sampled Likes
공개 Likes의 10% 샘플을 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `LikesSample10StreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:1706](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L1706)

___

### likesFirehose

▸ **likesFirehose**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream all Likes
모든 공개 Likes를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `LikesFirehoseStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:1983](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L1983)

___

### postsFirehoseJa

▸ **postsFirehoseJa**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream Japanese Posts
모든 공개 일본어 Posts를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsFirehoseJaStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:2257](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L2257)

___

### postsFirehoseEn

▸ **postsFirehoseEn**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream English Posts
모든 공개 영어 Posts를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsFirehoseEnStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:2571](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L2571)

___

### postsSample

▸ **postsSample**(`options?`): `Promise`\<`EventDrivenStream`\>

Stream sampled Posts
공개 Posts의 1% 샘플을 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `options` | `PostsSampleStreamingOptions` |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:2887](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L2887)

___

### postsSample10

▸ **postsSample10**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream 10% sampled Posts
공개 Posts의 10% 샘플을 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsSample10StreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:3145](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L3145)

___

### postsCompliance

▸ **postsCompliance**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream Posts compliance data
Posts와 관련된 모든 compliance 데이터를 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsComplianceStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:3459](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L3459)

___

### labelsCompliance

▸ **labelsCompliance**(`options?`): `Promise`\<`EventDrivenStream`\>

Stream Post labels
Posts에 적용된 모든 레이블링 이벤트를 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `options` | `LabelsComplianceStreamingOptions` |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:3651](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L3651)

___

### posts

▸ **posts**(`options?`): `Promise`\<`EventDrivenStream`\>

Stream filtered Posts
활성 규칙 세트와 일치하는 Posts를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `options` | `PostsStreamingOptions` |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:3827](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L3827)

___

### postsFirehose

▸ **postsFirehose**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream all Posts
모든 공개 Posts를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsFirehoseStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:4125](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L4125)

___

### postsFirehosePt

▸ **postsFirehosePt**(`partition`, `options?`): `Promise`\<`EventDrivenStream`\>

Stream Portuguese Posts
모든 공개 포르투갈어 Posts를 실시간으로 스트리밍합니다.

사용하기 쉬운 이벤트 기반 스트림을 반환합니다.
'data', 'error', 'close'와 같은 이벤트를 수신하려면 .on()을 사용하세요.
for await...of를 사용한 비동기 반복도 지원됩니다.

#### 매개변수

| 이름 | 타입 | 설명 |
| :------ | :------ | :------ |
| `partition` | `number` | 파티션 번호입니다. |
| `options` | `PostsFirehosePtStreamingOptions` | - |

#### 반환값

`Promise`\<`EventDrivenStream`\>

스트리밍 데이터를 처리하기 위한 이벤트 기반 스트림

#### 정의 위치

[stream/stream_client.ts:4439](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L4439)

___

### getRuleCounts

▸ **getRuleCounts**(`options?`): `Promise`\<[`Get2TweetsSearchStreamRulesCountsResponse`](/xdks/typescript/reference/interfaces/Schemas.Get2TweetsSearchStreamRulesCountsResponse)\>

Get stream rule counts
필터링된 스트림에 대한 활성 규칙 세트의 규칙 수를 검색합니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `options` | `GetRuleCountsStreamingOptions` |

#### 반환값

`Promise`\<[`Get2TweetsSearchStreamRulesCountsResponse`](/xdks/typescript/reference/interfaces/Schemas.Get2TweetsSearchStreamRulesCountsResponse)\>

API 응답을 포함하는 Promise

#### 정의 위치

[stream/stream_client.ts:4753](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L4753)

___

### getRules

▸ **getRules**(`options?`): `Promise`\<[`RulesLookupResponse`](/xdks/typescript/reference/interfaces/Schemas.RulesLookupResponse)\>

Get stream rules
필터링된 스트림에 대한 활성 규칙 세트 또는 규칙의 하위 집합을 검색합니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `options` | `GetRulesStreamingOptions` |

#### 반환값

`Promise`\<[`RulesLookupResponse`](/xdks/typescript/reference/interfaces/Schemas.RulesLookupResponse)\>

API 응답을 포함하는 Promise

#### 정의 위치

[stream/stream_client.ts:4854](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L4854)

___

### updateRules

▸ **updateRules**(`body`, `options?`): `Promise`\<[`AddOrDeleteRulesResponse`](/xdks/typescript/reference/interfaces/Schemas.AddOrDeleteRulesResponse)\>

Update stream rules
필터링된 스트림에 대한 활성 규칙 세트에서 규칙을 추가하거나 삭제합니다.

#### 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `body` | `any` |
| `options` | `UpdateRulesStreamingOptions` |

#### 반환값

`Promise`\<[`AddOrDeleteRulesResponse`](/xdks/typescript/reference/interfaces/Schemas.AddOrDeleteRulesResponse)\>

API 응답을 포함하는 Promise

#### 정의 위치

[stream/stream_client.ts:4987](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/stream/stream_client.ts#L4987)
