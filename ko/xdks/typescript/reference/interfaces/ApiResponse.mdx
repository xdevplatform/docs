---
title: "ApiResponse"
sidebarTitle: "ApiResponse"
description: "X API SDK의 ApiResponse TypeScript 인터페이스 참조. X API 요청 또는 응답 페이로드의 필드 정의, 타입 및 속성."
---

메타데이터가 포함된 응답 래퍼

## 타입 매개변수

| 이름 | 타입 |
| :------ | :------ |
| `T` | `any` |

## 속성

<ResponseField name="body" type="T" required>
응답 본문

</ResponseField>

<ResponseField name="headers" type="Headers" required>
응답 헤더

</ResponseField>

<ResponseField name="status" type="number" required>
HTTP 상태 코드

</ResponseField>

<ResponseField name="statusText" type="string" required>
HTTP 상태 텍스트

</ResponseField>

<ResponseField name="url" type="string" required>
응답 URL

</ResponseField>
