---
title: "TypeScript XDK"
sidebarTitle: "Descripción general"
description: "Descripción general del TypeScript XDK para la X API v2, con paginación inteligente, OAuth 1.0a, OAuth 2.0, autenticación con Bearer Token, streaming en tiempo real y seguridad de tipos completa."
---

Un SDK integral de TypeScript para la X API (anteriormente Twitter API) con funciones avanzadas que incluyen paginación inteligente, múltiples métodos de autenticación, streaming en tiempo real y seguridad de tipos completa.

## Características principales

- **🔐 Autenticación**: Contexto de usuario (OAuth 1.0a, OAuth 2.0) y autenticación App-Only (Bearer Token)
- **🔄 Paginación**: Paginación automática con soporte para iteración asíncrona
- **📡 Streaming**: Streaming basado en eventos con reconexión automática
- **📚 Seguridad de tipos**: Definiciones completas de TypeScript para todos los endpoints y parámetros
- **🎯 Soporte completo de la X API**: Users, Posts, Lists, Bookmarks, Communities y más

## Inicio rápido

<CodeGroup dropdown>

```typescript title="quickstart.ts" lines wrap icon="square-js" theme={null}
import { 
    Client, 
    type ClientConfig,
    type Users
} from '@xdevplatform/xdk';

const config: ClientConfig = { bearerToken: 'your-bearer-token' };

const client: Client = new Client(config);

async function main(): Promise<void> {
  const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
  const username: string = userResponse.data?.username!;
  console.log(username);
}

main();
```

```javascript quickstart.js theme={null}
import { Client } from '@xdevplatform/xdk';

const client = new Client({ bearerToken: 'your-bearer-token' });

const userResponse = await client.users.getByUsername('XDevelopers');
const username = userResponse.data.username;
console.log(username);
```

</CodeGroup>

<Info>
Para ejemplos de código detallados usando el XDK de JavaScript/TypeScript, consulta nuestro [repositorio de ejemplos de código en GitHub](https://github.com/xdevplatform/samples/tree/main/javascript).
</Info>

## ¿Qué sigue?

- [Guía de instalación](/xdks/typescript/install) - Configura el SDK en tu proyecto
- [Autenticación](/xdks/typescript/authentication) - Aprende sobre los diferentes métodos de autenticación
- [Paginación](/xdks/typescript/pagination) - Aprende sobre la paginación de datos
- [Streaming](/xdks/typescript/streaming) - Aprende sobre el streaming de datos en tiempo real
- [Referencia de la API](/xdks/typescript/reference/classes/Client) - Lee la documentación completa de la API
