---
title: "ApiError"
sidebarTitle: "ApiError"
description: "Referencia de la clase ApiError en el SDK de TypeScript de la X API. Propiedades y métodos para inspeccionar el estado HTTP, códigos de error y respuestas de error de la X API."
---

Clase de error de API para manejar errores de la X API

## Jerarquía

- `Error`

  ↳ **`ApiError`**

## Constructores

### constructor

• **new ApiError**(`message`, `status`, `statusText`, `headers`, `data?`): [`ApiError`](/xdks/typescript/reference/classes/ApiError)

#### Parámetros

| Nombre | Tipo |
| :------ | :------ |
| `message` | `string` |
| `status` | `number` |
| `statusText` | `string` |
| `headers` | `Headers` |
| `data?` | `any` |

#### Devuelve

[`ApiError`](/xdks/typescript/reference/classes/ApiError)

#### Sobrescribe

Error.constructor



[client.ts:123](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/client.ts#L123)

## Propiedades

<ResponseField name="status" type="number" required>
</ResponseField>

<ResponseField name="statusText" type="string" required>
</ResponseField>

<ResponseField name="headers" type="Headers" required>
</ResponseField>

<ResponseField name="data" type="any">
</ResponseField>
