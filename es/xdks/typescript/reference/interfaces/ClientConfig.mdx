---
title: "ClientConfig"
sidebarTitle: "ClientConfig"
description: "Referencia de la interfaz TypeScript ClientConfig en el X API SDK. Opciones de configuración para el cliente de la X API. Bearer Token para autenticación OAuth1."
---

Opciones de configuración para el cliente de la X API

## Propiedades

<ResponseField name="baseUrl" type="string">
URL base para las solicitudes de la API

</ResponseField>

<ResponseField name="bearerToken" type="string">
Bearer Token para la autenticación

</ResponseField>

<ResponseField name="accessToken" type="string">
Token de acceso de OAuth 2.0

</ResponseField>

<ResponseField name="oauth1" type="any">
Instancia de OAuth1 para la autenticación

</ResponseField>

<ResponseField name="headers" type="Record&lt;string, string&gt;">
Encabezados personalizados para incluir en las solicitudes

</ResponseField>

<ResponseField name="timeout" type="number">
Tiempo de espera de la solicitud en milisegundos

</ResponseField>

<ResponseField name="retry" type="boolean">
Si se deben reintentar automáticamente las solicitudes fallidas

</ResponseField>

<ResponseField name="maxRetries" type="number">
Número máximo de reintentos

</ResponseField>
