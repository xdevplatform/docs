---
title: OAuth 1.0a
description: "Descripción general de la autenticación de contexto de usuario con OAuth 1.0a en X, cubriendo claves de API, access tokens, firma de solicitudes y herramientas para hacer llamadas autorizadas a la API."
sidebarTitle: Descripción general
keywords: ["OAuth 1.0a", "OAuth 1.0", "autenticación OAuth 1.0a", "contexto de usuario OAuth 1.0a", "OAuth de tres piernas", "flujo OAuth 1.0a"]
---

import { Button } from "/snippets/button.mdx";

## OAuth 1.0a

Muchos endpoints de la plataforma para desarrolladores de X usan el método OAuth 1.0a para actuar, o hacer solicitudes a la API, en nombre de una cuenta de X. Por ejemplo, si tienes una app de desarrollador de X, puedes hacer solicitudes a la API en nombre de cualquier cuenta de X siempre que ese usuario autentique tu app.

Ten en cuenta: si no estás familiarizado con conceptos como HMAC-SHA1 y percent encoding, te recomendamos que consultes la sección "herramientas útiles" a continuación, que enumera algunos clientes de API que simplifican en gran medida el proceso de autenticación.

### Conceptos clave

#### Firmar una solicitud con claves y tokens

Debes firmar cada solicitud a la API pasando varias claves y tokens generados en un authorization header. Para empezar, puedes generar varias claves y tokens en la página de detalles de tu [X developer app](/resources/fundamentals/developer-apps), incluidos los siguientes:

|     |     |
| :--- | :--- |
| API key y secret:<br/><br/>`oauth_consumer_key`<br/><br/>`oauth_consumer_secret` | Piensa en estos como el nombre de usuario y la contraseña que representan tu X developer app al hacer solicitudes a la API. |
| Access token y secret:<br/><br/>`oauth_token`<br/><br/>`oauth_token_secret` | Un access token y un access token secret son credenciales específicas de usuario que se utilizan para autenticar las solicitudes a la API OAuth 1.0a. Especifican la cuenta de X en cuyo nombre se realiza la solicitud.<br/><br/>Puedes generar tu propio access token y token secret si deseas que tu app realice solicitudes en nombre de la misma cuenta de X asociada a tu cuenta de desarrollador desde la página de detalles de la [X developer app](/resources/fundamentals/developer-apps).<br/><br/>Si deseas generar access tokens para un usuario diferente, consulta "Hacer solicitudes en nombre de los usuarios" más abajo. |

#### Hacer solicitudes en nombre de los usuarios

Al crear una firma, necesitas un conjunto de access tokens que representen al usuario en cuyo nombre vas a hacer una solicitud.

Puedes generar un conjunto de access tokens que represente la cuenta de X propietaria de la X developer app desde la [página de detalles de la app,](https://developer.x.com/content/developer-twitter/en/apps) pero si deseas hacer una solicitud en nombre de otra cuenta de X, el propietario de esa cuenta debe otorgarte acceso iniciando sesión en su cuenta como parte del [flujo de 3-legged OAuth](/resources/fundamentals/authentication#obtaining-access-tokens-using-3-legged-oauth-flow). El resultado de este proceso es un conjunto de access tokens (oauth\_token y oauth\_token_secret) que se puede usar para hacer una solicitud OAuth 1.0a.

Una vez que tengas estas claves y tokens, puedes [crear una firma](/resources/fundamentals/authentication#creating-a-signature) desde cero. No recomendamos hacerlo a menos que sepas lo que haces, o utilizar una de las herramientas mencionadas a continuación para hacer una solicitud a un endpoint que requiera OAuth 1.0a.

Como referencia, aquí tienes un ejemplo de solicitud cURL firmada con todos los tokens generados pasados en un authorization header:

```bash
curl --request POST \
  --url 'https://api.x.com/1.1/statuses/update.json?status=Hello%20world' \
  --header 'authorization: OAuth oauth_consumer_key="CONSUMER_API_KEY", oauth_nonce="OAUTH_NONCE", oauth_signature="OAUTH_SIGNATURE", oauth_signature_method="HMAC-SHA1", oauth_timestamp="OAUTH_TIMESTAMP", oauth_token="ACCESS_TOKEN", oauth_version="1.0"' \
```

<Note>
**Nota:** Los access tokens de usuario son sensibles y deben protegerse con mucho cuidado. Cuando se generan access tokens, el usuario que representan confía en que tu aplicación los mantendrá seguros. Si tanto las API keys como los access tokens de usuario se ven comprometidos, tu aplicación podría exponer potencialmente el acceso a información privada y a funciones de la cuenta. Te animamos a que aprendas más sobre [proteger claves y access tokens](/resources/fundamentals/developer-apps).
</Note>

### Herramientas útiles

El proceso de [firmar una solicitud](/resources/fundamentals/authentication#creating-a-signature) es complicado. Recomendamos que uses una biblioteca cliente de API que genere automáticamente gran parte del token de autenticación:

|     |     |
| :--- | :--- |
| [Postman](https://www.getpostman.com/) | Un cliente de API que te permite construir y enviar solicitudes de REST API. Lee nuestro tutorial "[Getting started with Postman](/tutorials/postman-getting-started)" para aprender más sobre esta herramienta. |
| [Insomnia](https://insomnia.rest/) | Insomnia es un cliente de REST API con gestión de cookies, variables de entorno, generación de código y autenticación para Mac, Window y Linux. |
