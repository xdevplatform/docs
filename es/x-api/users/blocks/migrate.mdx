---
title: Guía de migración
sidebarTitle: Guía de migración
keywords: ["migración de blocks", "migrar blocks", "v1.1 a v2 blocks", "guía de migración de blocks", "migrar blocks"]
description: "Los endpoints para bloquear y desbloquear usuarios solo están disponibles en el plan Enterprise. Referencia del nivel estándar de X API v2 sobre blocks."
---


import { Button } from '/snippets/button.mdx';

<Callout icon="/icons/xds/icon-key.svg" color="#22C55E" iconType="regular">
Los endpoints para bloquear y desbloquear usuarios solo están disponibles en el plan Enterprise. Puedes completar el formulario de interés Enterprise [aquí](/forms/enterprise-api-interest).
</Callout>

### Blocks lookup: Standard v1.1 comparado con X API v2

Si has estado trabajando con los endpoints estándar v1.1 [GET blocks/ids](https://developer.x.com/en/docs/twitter-api/v1/accounts-and-users/mute-block-report-users/api-reference/get-blocks-ids) y [GET blocks/list](https://developer.x.com/en/docs/twitter-api/v1/accounts-and-users/mute-block-report-users/api-reference/get-blocks-list), el objetivo de esta guía es ayudarte a entender las similitudes y diferencias entre los endpoints estándar v1.1 y de X API v2 para blocks lookup.

* **Similitudes**
    * Autenticación
* **Diferencias**
    * URLs de endpoints

    * Límites de usuarios por solicitud
    * Requisitos de App y Project
    * Formatos de datos de respuesta
    * Parámetros de solicitud

#### Similitudes

**Autenticación**

Tanto los endpoints estándar v1.1 como los de X API v2 de blocks lookup usan [OAuth 1.0a User Context](/resources/fundamentals/authentication#oauth-1-0a-2). Por lo tanto, si anteriormente usabas uno de los endpoints estándar v1.1 de blocks lookup, puedes seguir utilizando el mismo método de autenticación si migras a la versión de X API v2.

#### Diferencias
**URLs de endpoints**

* Endpoints estándar v1.1:
    * GET https://api.x.com/1.1/blocks/ids.json
        (lista de user IDs que están bloqueados por el usuario especificado)
    * GET https://api.x.com/1.1/blocks/lists.json
        (lista de usuarios que están bloqueados por el usuario especificado)
* Endpoint X API v2:
    * GET https://api.x.com/2/users/:id/blocking
        (lista de usuarios que están bloqueados por el user ID especificado)


**Límites de usuarios por solicitud**

Los endpoints estándar v1.1 te permiten devolver hasta 5000 usuarios por solicitud. Los nuevos endpoints v2 te permiten devolver hasta 1000 usuarios por solicitud. Para devolver los 1000 usuarios completos, deberás pasar max_results=1000 como parámetro de consulta; luego puedes pasar el next_token devuelto en el payload de respuesta al parámetro de consulta pagination_token en tu siguiente solicitud.


**Requisitos de App y Project**

Los endpoints de X API v2 requieren que uses credenciales de una [App de desarrollador](/resources/fundamentals/developer-apps) asociada a un [Project](/resources/fundamentals/developer-apps) al autenticar tus solicitudes. Todos los endpoints de X API v1.1 pueden usar credenciales de Apps o Apps asociadas a un project.


**Formato de datos de respuesta**

Una de las mayores diferencias entre las versiones estándar v1.1 y X API v2 del endpoint es cómo seleccionas qué fields se devuelven en tu payload.

Para los endpoints estándar, recibes muchos de los fields de respuesta por defecto y tienes la opción de usar parámetros para identificar qué fields o conjuntos de fields deben devolverse en el payload.

La versión de X API v2 solo entrega los fields id, name y username del usuario por defecto. Para solicitar fields u objetos adicionales, deberás usar los parámetros [fields](/x-api/fundamentals/fields) y [expansions](/x-api/fundamentals/expansions). Cualquier field de usuario que solicites desde este endpoint se devolverá en el objeto de usuario principal. Cualquier objeto Post expandido y sus fields se devolverán en un objeto includes dentro de tu respuesta. Luego puedes emparejar los objetos expandidos con el objeto de usuario coincidiendo los IDs ubicados tanto en el objeto de usuario como en el objeto Post expandido.

Te animamos a leer más sobre estos nuevos parámetros en sus respectivas guías, o leyendo nuestra guía sobre [cómo usar fields y expansions](/x-api/fundamentals/data-dictionary/reference#how-to-use-fields-and-expansions).

También hemos elaborado una [guía de migración de formatos de datos](/x-api/migrate/data-format-migration#migrating-from-standard-v1-1s-data-format-to-v2) que puede ayudarte a mapear fields estándar v1.1 a los nuevos fields v2. Esta guía también te proporcionará los parámetros expansion y field específicos que necesitarás pasar con tu solicitud v2 para devolver fields específicos.


Además de los cambios en cómo solicitas ciertos fields, X API v2 también introduce nuevos diseños JSON para los objetos devueltos por las APIs, incluyendo objetos [P](/x-api/fundamentals/data-dictionary/reference#tweet)ost y [user](/x-api/fundamentals/data-dictionary/reference#user).

* En el nivel raíz del JSON, los endpoints estándar devuelven objetos Post en un array statuses, mientras que X API v2 devuelve un array data.
* En lugar de referirse a "statuses" Retweeted y Quoted, el JSON de X API v2 se refiere a Tweets Retweeted y Quoted. Se eliminan muchos fields heredados y deprecados, como contributors y user.translator_type.
* En lugar de usar tanto favorites (en el objeto Post) como favourites (en el objeto de usuario), X API v2 usa el término like.
* X está adoptando la convención de que los valores JSON sin valor (por ejemplo, null) no se escriben en el payload. Los atributos de Post y usuario solo se incluyen si tienen valores no nulos.


También introdujimos un nuevo conjunto de fields en el [objeto Post](/x-api/fundamentals/data-dictionary/reference#tweet), incluyendo los siguientes:

* Un field [conversation_id](/x-api/fundamentals/conversation-id)
* Dos nuevos fields de [annotations](/x-api/fundamentals/post-annotations), incluyendo context y entities
* Varios nuevos fields de [metrics](/x-api/fundamentals/metrics)
* Un nuevo field reply_setting, que muestra quién puede responder a un Post determinado

**Parámetros de solicitud**

Los siguientes parámetros de solicitud estándar v1.1 aceptaban dos parámetros de consulta de solicitud (user_id o screen_name). X API v2 solo acepta el user ID numérico, y debe pasarse como parte de la ruta del endpoint.

---

## Ejemplos de código

### Obtener usuarios bloqueados (v2)

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/123456789/blocking?user.fields=username,verified&max_results=100" \
  -H "Authorization: OAuth ..."
```

```python title="Python" lines wrap icon="python"
# Requiere autenticación OAuth 1.0a User Context
import requests
from requests_oauthlib import OAuth1

auth = OAuth1(
    "API_KEY", "API_SECRET",
    "ACCESS_TOKEN", "ACCESS_TOKEN_SECRET"
)

url = "https://api.x.com/2/users/123456789/blocking"
params = {"user.fields": "username,verified", "max_results": 100}

response = requests.get(url, auth=auth, params=params)
print(response.json())
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Obtener usuarios bloqueados con paginación
for page in client.users.get_blocking(
    "123456789",
    user_fields=["username", "verified"],
    max_results=100
):
    for user in page.data:
        print(f"{user.username} - Verified: {user.verified}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Obtener usuarios bloqueados con paginación
const paginator = client.users.getBlocking("123456789", {
  userFields: ["username", "verified"],
  maxResults: 100,
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(`${user.username} - Verified: ${user.verified}`);
  });
}
```

</CodeGroup>

### Bloquear a un usuario (v2) — Solo Enterprise

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/users/123456789/blocking" \
  -H "Authorization: OAuth ..." \
  -H "Content-Type: application/json" \
  -d '{"target_user_id": "2244994945"}'
```

```python title="Python" lines wrap icon="python"
# Requiere autenticación OAuth 1.0a User Context
import requests
from requests_oauthlib import OAuth1

auth = OAuth1(
    "API_KEY", "API_SECRET",
    "ACCESS_TOKEN", "ACCESS_TOKEN_SECRET"
)

url = "https://api.x.com/2/users/123456789/blocking"
response = requests.post(url, auth=auth, json={"target_user_id": "2244994945"})
print(response.json())
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Bloquear a un usuario
response = client.users.block(
    source_user_id="123456789",
    target_user_id="2244994945"
)
print(f"Blocking: {response.data.blocking}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Bloquear a un usuario
const response = await client.users.block("123456789", {
  targetUserId: "2244994945",
});
console.log(`Blocking: ${response.data?.blocking}`);
```

</CodeGroup>
