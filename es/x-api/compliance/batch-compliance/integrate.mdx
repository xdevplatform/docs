---
title: Guía de integración
sidebarTitle: Guía de integración
keywords: ["integración de batch compliance", "integración batch de compliance", "integración de compliance jobs", "configuración de batch compliance"]
description: "Al usar los endpoints de Batch compliance, los desarrolladores pueden subir en lote grandes cantidades de. Referencia del nivel estándar de X API v2 que cubre batch compliance."


---
import { Button } from '/snippets/button.mdx';

## Trabajar con cargas reanudables

Al usar los endpoints de Batch compliance, los desarrolladores pueden subir en lote grandes cantidades de datos de X y comprender qué acción se requiere para asegurar que sus conjuntos de datos reflejen la intención del usuario y el estado actual del contenido en X. Subir grandes cantidades de datos a un servidor remoto es una operación relativamente sencilla cuando los sistemas y la conectividad son estables y confiables. Sin embargo, esto no siempre es así. Algunos entornos pueden imponer un tiempo de espera de conexión, cortando efectivamente la conexión entre tu app y el servidor de subida después de un tiempo determinado; también puedes encontrar problemas de conexión, por ejemplo al intentar subir un archivo grande desde tu laptop a través de una conexión wi-fi. En estas circunstancias, es deseable subir porciones más pequeñas de ese archivo a la vez, en lugar de tener una sola conexión continua.

Los endpoints de batch compliance de X se basan en Google Cloud Storage para procesar archivos grandes. Este tipo de almacenamiento está optimizado para diversas aplicaciones; Cloud Storage admite una técnica para gestionar archivos grandes llamada cargas reanudables.

Si la carga falla en algún punto, Google Cloud Storage es capaz de reanudar la operación desde donde se dejó.

### Crear un job reanudable

#### Paso uno:

Primero, tendrás que crear un compliance job y especificar si vas a subir Post IDs o user IDs (usando el parámetro type). Además, agrega resumable al cuerpo y establécelo en true. Asegúrate de reemplazar el $APP\_ACCESS\_TOKEN a continuación con tu App only Access Token.

```
curl --request POST \
 'https://api.x.com/2/compliance/jobs' --header 'Authorization: Bearer $APP_ACCESS_TOKEN --header 'Content-Type: application/json' --data-raw '{
   "type": "tweets",
  
