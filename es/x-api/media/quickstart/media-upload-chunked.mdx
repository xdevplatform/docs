---
title: Carga de medios por partes (Chunked)
sidebarTitle: Quickstart
description: "Esta guía te lleva paso a paso a subir videos y archivos de medios grandes usando la carga por partes. Referencia para el nivel estándar de la X API v2 que cubre quickstart."
keywords: ["chunked upload", "carga de medios", "medios por partes", "subir video", "carga de archivo grande", "guía de chunked upload"]
---

Esta guía te lleva paso a paso a subir videos y archivos de medios grandes usando el flujo de trabajo de carga por partes (chunked).

Para cargas de video o de medios grandes, debes:
1. **INIT** — Inicializar la carga y obtener un `media_id`
2. **APPEND** — Subir cada chunk del archivo
3. **FINALIZE** — Completar la carga
4. **STATUS** — (Si es necesario) Esperar a que se complete el procesamiento

<Note>
Consulta [este código de ejemplo](https://github.com/xdevplatform/large-video-upload-python) para un ejemplo completo en Python.
</Note>

---

## Paso 1: Inicializar la carga (INIT)

Inicia la sesión de carga para obtener un `media_id`:

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/media/upload" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: multipart/form-data" \
  -F "command=INIT" \
  -F "media_type=video/mp4" \
  -F "total_bytes=1048576" \
  -F "media_category=amplify_video"
```

```python title="SDK de Python" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Initialize chunked upload
response = client.media.init_upload(
    media_type="video/mp4",
    total_bytes=1048576,
    media_category="amplify_video"
)

media_id = response.data.id
print(f"Media ID: {media_id}")
```

```javascript title="SDK de JavaScript" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Initialize chunked upload
const response = await client.media.initUpload({
  mediaType: "video/mp4",
  totalBytes: 1048576,
  mediaCategory: "amplify_video",
});

const mediaId = response.data?.id;
console.log(`Media ID: ${mediaId}`);
```

</CodeGroup>

**Respuesta:**

```json
{
  "data": {
    "id": "1880028106020515840",
    "media_key": "13_1880028106020515840",
    "expires_after_secs": 1295999
  }
}
```

---

## Paso 2: Subir chunks (APPEND)

Sube cada chunk del archivo. Por ejemplo, divide un archivo de 3 MB en 3 chunks:

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/media/upload" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: multipart/form-data" \
  -F "command=APPEND" \
  -F "media_id=1880028106020515840" \
  -F "segment_index=0" \
  -F "media=@/path/to/chunk1.mp4"
```

```python title="SDK de Python" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Upload chunks
chunk_size = 1024 * 1024  # 1 MB chunks

with open("video.mp4", "rb") as f:
    segment_index = 0
    while True:
        chunk = f.read(chunk_size)
        if not chunk:
            break
        
        client.media.append_upload(
            media_id=media_id,
            segment_index=segment_index,
            media=chunk
        )
        segment_index += 1
        print(f"Uploaded chunk {segment_index}")
```

```javascript title="SDK de JavaScript" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";
import fs from "fs";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Upload chunks
const chunkSize = 1024 * 1024; // 1 MB chunks
const fileBuffer = fs.readFileSync("video.mp4");

let segmentIndex = 0;
for (let offset = 0; offset < fileBuffer.length; offset += chunkSize) {
  const chunk = fileBuffer.slice(offset, offset + chunkSize);
  
  await client.media.appendUpload({
    mediaId,
    segmentIndex,
    media: chunk,
  });
  
  console.log(`Uploaded chunk ${segmentIndex + 1}`);
  segmentIndex++;
}
```

</CodeGroup>

<Info>
**Ventajas del chunking:**
- Mayor fiabilidad en redes lentas
- Las cargas se pueden pausar y reanudar
- Los chunks fallidos se pueden reintentar individualmente
</Info>

---

## Paso 3: Finalizar la carga (FINALIZE)

Completa la carga después de que se hayan enviado todos los chunks:

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/media/upload" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: multipart/form-data" \
  -F "command=FINALIZE" \
  -F "media_id=1880028106020515840"
```

```python SDK de Python
from xdk import Client

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Finalize upload
response = client.media.finalize_upload(media_id=media_id)

print(f"Processing state: {response.data.processing_info.state}")
```

```javascript SDK de JavaScript
import { Client } from "@xdevplatform/xdk";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Finalize upload
const response = await client.media.finalizeUpload({ mediaId });

console.log(`Processing state: ${response.data?.processing_info?.state}`);
```

</CodeGroup>

**Respuesta:**

```json title="Ejemplo de respuesta" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": {
    "id": "1880028106020515840",
    "media_key": "13_1880028106020515840",
    "size": 1048576,
    "expires_after_secs": 86400,
    "processing_info": {
      "state": "pending",
      "check_after_secs": 1
    }
  }
}
```

<Note>
Si se devuelve `processing_info`, procede al Paso 4 para esperar el procesamiento. Si no, el medio está listo para usarse.
</Note>

---

## Paso 4: Comprobar el estado (STATUS)

Si se devolvió `processing_info`, sondea hasta que se complete el procesamiento:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/media/upload?command=STATUS&media_id=1880028106020515840" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="SDK de Python" lines wrap icon="python"
from xdk import Client
import time

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Wait for processing to complete
while True:
    response = client.media.get_status(media_id=media_id)
    state = response.data.processing_info.state
    
    if state == "succeeded":
        print("Media ready!")
        break
    elif state == "failed":
        print("Processing failed")
        break
    else:
        check_after = response.data.processing_info.check_after_secs
        print(f"Processing... checking again in {check_after}s")
        time.sleep(check_after)
```

```javascript title="SDK de JavaScript" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Wait for processing to complete
while (true) {
  const response = await client.media.getStatus({ mediaId });
  const state = response.data?.processing_info?.state;
  
  if (state === "succeeded") {
    console.log("Media ready!");
    break;
  } else if (state === "failed") {
    console.log("Processing failed");
    break;
  } else {
    const checkAfter = response.data?.processing_info?.check_after_secs ?? 1;
    console.log(`Processing... checking again in ${checkAfter}s`);
    await new Promise((r) => setTimeout(r, checkAfter * 1000));
  }
}
```

</CodeGroup>

**Estados de procesamiento:** `pending` → `in_progress` → `succeeded` o `failed`

---

## Paso 5: Crear un Post con medios

Una vez completado el procesamiento, crea un Post con el medio:

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/tweets" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Check out this video!",
    "media": {
      "media_ids": ["1880028106020515840"]
    }
  }'
```

```python SDK de Python
from xdk import Client

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Create Post with media
response = client.posts.create(
    text="Check out this video!",
    media={"media_ids": [media_id]}
)

print(f"Posted: {response.data.id}")
```

```javascript SDK de JavaScript
import { Client } from "@xdevplatform/xdk";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Create Post with media
const response = await client.posts.create({
  text: "Check out this video!",
  media: { mediaIds: [mediaId] },
});

console.log(`Posted: ${response.data?.id}`);
```

</CodeGroup>

---

## Categorías de medios

| Categoría | Descripción |
|:---------|:------------|
| `tweet_image` | Imagen para un Post |
| `tweet_gif` | GIF animado para un Post |
| `tweet_video` | Video para un Post |
| `amplify_video` | Video Amplify |

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Buenas prácticas" icon="/icons/xds/icon-book.svg" href="/x-api/media/quickstart/best-practices">
    Restricciones y requisitos de archivo
  </Card>
  <Card title="Crear Posts" icon="/icons/xds/icon-chat.svg" href="/x-api/posts/manage-tweets/quickstart">
    Publica con medios
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/media/initialize-media-upload">
    Documentación completa de endpoints
  </Card>
</CardGroup>
