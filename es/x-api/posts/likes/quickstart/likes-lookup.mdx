---
title: Likes Lookup
sidebarTitle: Likes Lookup
description: "Quickstart para recuperar datos de likes de la X API, incluidos los usuarios que dieron like y los Posts con like, usando los endpoints del nivel estándar de X API v2 para likes lookup."
keywords: ["likes lookup", "quickstart de likes", "obtener likes", "tutorial de likes", "guía de likes", "ejemplo de likes"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te muestra cómo recuperar datos de likes usando la X API.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- El Bearer Token de tu App (para datos públicos) o un User Access Token (para métricas privadas)
</Note>

---

## Obtener los usuarios que dieron like a un Post

Recupera la lista de usuarios que dieron like a un Post específico:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/1354143047324299264/liking_users?\
user.fields=created_at,username,verified" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python Python SDK
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén los usuarios que dieron like a un Post con paginación
for page in client.posts.get_liking_users(
    "1354143047324299264",
    user_fields=["created_at", "username", "verified"]
):
    for user in page.data:
        print(f"{user.username} - Joined: {user.created_at}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén los usuarios que dieron like a un Post con paginación
const paginator = client.posts.getLikingUsers("1354143047324299264", {
  userFields: ["created_at", "username", "verified"],
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(`${user.username} - Joined: ${user.created_at}`);
  });
}
```

</CodeGroup>

### Respuesta

```json title="Ejemplo de respuesta" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": [
    {
      "created_at": "2008-12-04T18:51:57.000Z",
      "id": "17874544",
      "username": "TwitterSupport",
      "name": "Twitter Support",
      "verified": true
    },
    {
      "created_at": "2007-02-20T14:35:54.000Z",
      "id": "783214",
      "username": "Twitter",
      "name": "Twitter",
      "verified": true
    }
  ],
  "meta": {
    "result_count": 2,
    "next_token": "7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix"
  }
}
```

---

## Obtener los Posts a los que dio like un usuario

Recupera los Posts a los que un usuario específico ha dado like:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/2244994945/liked_tweets?\
tweet.fields=created_at,public_metrics&\
max_results=10" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén los Posts a los que un usuario dio like con paginación
for page in client.users.get_liked_tweets(
    "2244994945",
    tweet_fields=["created_at", "public_metrics"],
    max_results=10
):
    for post in page.data:
        print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén los Posts a los que un usuario dio like con paginación
const paginator = client.users.getLikedTweets("2244994945", {
  tweetFields: ["created_at", "public_metrics"],
  maxResults: 10,
});

for await (const page of paginator) {
  page.data?.forEach((post) => {
    console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
  });
}
```

</CodeGroup>

### Respuesta

```json title="Ejemplo de respuesta" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": [
    {
      "id": "1362449997430542337",
      "text": "Honored to be the first developer to be featured...",
      "created_at": "2021-02-18T17:45:00.000Z",
      "public_metrics": {
        "retweet_count": 5,
        "reply_count": 2,
        "like_count": 42,
        "quote_count": 1
      }
    }
  ],
  "meta": {
    "result_count": 1,
    "next_token": "7140dibdnow9c7btw4539n0vybdnx19ylpayqf16fjt4l"
  }
}
```

---

## Incluir datos adicionales

Usa expansions para obtener datos relacionados, como los Posts fijados:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/1354143047324299264/liking_users?\
user.fields=created_at&\
expansions=pinned_tweet_id&\
tweet.fields=created_at" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén los usuarios que dieron like con expansion de Post fijado
for page in client.posts.get_liking_users(
    "1354143047324299264",
    user_fields=["created_at"],
    expansions=["pinned_tweet_id"],
    tweet_fields=["created_at"]
):
    for user in page.data:
        print(f"{user.username}")
    # Los Posts fijados están en page.includes.tweets
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén los usuarios que dieron like con expansion de Post fijado
const paginator = client.posts.getLikingUsers("1354143047324299264", {
  userFields: ["created_at"],
  expansions: ["pinned_tweet_id"],
  tweetFields: ["created_at"],
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(user.username);
  });
  // Los Posts fijados están en page.includes?.tweets
}
```

</CodeGroup>

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Gestionar likes" icon="/icons/xds/icon-heart.svg" href="/x-api/posts/likes/quickstart/manage-likes">
    Da y quita likes a Posts
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/posts/get-liking-users">
    Documentación completa del endpoint
  </Card>
</CardGroup>
