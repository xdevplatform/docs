---
title: Consulta de Bookmarks
sidebarTitle: Consulta de Bookmarks
description: "Esta guía te muestra cómo recuperar tus Posts guardados como bookmarks usando la X API. Referencia del nivel estándar de X API v2 sobre quickstart."
keywords: ["quickstart de consulta de bookmarks", "quickstart de bookmark lookup", "tutorial de bookmark lookup", "guía de bookmark lookup"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te muestra cómo recuperar tus Posts guardados como bookmarks usando la X API.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- Un User Access Token con el scope `bookmark.read` (OAuth 2.0 PKCE)
</Note>

---

## Obtén tus bookmarks

<Steps>
  <Step title="Obtén tu ID de usuario">
    Necesitas el ID de tu usuario autenticado. Puedes encontrarlo usando el endpoint `/2/users/me` o desde el [endpoint de user lookup](/x-api/users/lookup/introduction).
  </Step>

  <Step title="Solicita tus bookmarks">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/2244994945/bookmarks?\
tweet.fields=created_at,public_metrics,author_id&\
max_results=10" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Obtén los Posts guardados como bookmarks con paginación
for page in client.bookmarks.get(
    "2244994945",
    tweet_fields=["created_at", "public_metrics", "author_id"],
    max_results=10
):
    for post in page.data:
        print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Obtén los Posts guardados como bookmarks con paginación
const paginator = client.bookmarks.get("2244994945", {
  tweetFields: ["created_at", "public_metrics", "author_id"],
  maxResults: 10,
});

for await (const page of paginator) {
  page.data?.forEach((post) => {
    console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
  });
}
```

</CodeGroup>

  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": [
        {
          "id": "1501258597237342208",
          "text": "Have you built a project using the X API you'd like to share with the community? We'd love to hear from you!",
          "created_at": "2024-01-15T10:30:00.000Z",
          "author_id": "2244994945",
          "public_metrics": {
            "retweet_count": 15,
            "reply_count": 8,
            "like_count": 89,
            "quote_count": 3
          }
        },
        {
          "id": "1501258542258348032",
          "text": "This is just one way developer innovation helps make X a better place...",
          "created_at": "2024-01-15T09:15:00.000Z",
          "author_id": "2244994945",
          "public_metrics": {
            "retweet_count": 22,
            "reply_count": 5,
            "like_count": 156,
            "quote_count": 7
          }
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "7140dibdnow9c7btw4539n0vybdnx19ylpayqf16fjt4l"
      }
    }
    ```
  </Step>
</Steps>

---

## Incluye información del autor

Usa expansions para obtener datos sobre los autores de los Posts:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/2244994945/bookmarks?\
tweet.fields=created_at,author_id&\
expansions=author_id&\
user.fields=username,verified" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

# Obtén los bookmarks con información del autor
for page in client.bookmarks.get(
    "2244994945",
    tweet_fields=["created_at", "author_id"],
    expansions=["author_id"],
    user_fields=["username", "verified"]
):
    for post in page.data:
        print(f"{post.text[:50]}...")
    # La información del autor está en page.includes.users
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

// Obtén los bookmarks con información del autor
const paginator = client.bookmarks.get("2244994945", {
  tweetFields: ["created_at", "author_id"],
  expansions: ["author_id"],
  userFields: ["username", "verified"],
});

for await (const page of paginator) {
  page.data?.forEach((post) => {
    console.log(`${post.text?.slice(0, 50)}...`);
  });
  // La información del autor está en page.includes?.users
}
```

</CodeGroup>

---

## Scopes requeridos

Al usar OAuth 2.0 PKCE, tu access token debe tener estos scopes:

| Scope | Description |
|:------|:------------|
| `bookmark.read` | Leer bookmarks |
| `tweet.read` | Leer datos de Posts |
| `users.read` | Leer datos de usuarios (para expansions) |

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Administrar bookmarks" icon="bookmark" href="/x-api/posts/bookmarks/quickstart/manage-bookmarks">
    Agrega y elimina bookmarks
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/users/get-bookmarks">
    Documentación completa del endpoint
  </Card>
</CardGroup>
