---
title: v1 a v2 (Enterprise)
sidebarTitle: v1 a v2 (Enterprise)
keywords: ["enterprise a v2", "migración enterprise", "GNIP a v2", "migración de enterprise search", "migrar enterprise", "v1 a v2 enterprise"]
description: "Migra desde la Enterprise Search Posts API a X API v2 con una comparación lado a lado de paginación, zonas horarias, historial de edición de Post y campos de metadatos."


---
### Enterprise comparado con X API v2


**Similitudes**

* Paginación
* Zona horaria
* Soporte para historial y metadatos de edición de Post. 

**Diferencias**

* URLs de endpoint
* Requisito de App y Project
* Períodos de tiempo disponibles
* Formato de datos de respuesta
* Métodos HTTP
* Formatos de tiempo de solicitud
* Parámetros de solicitud
* Operadores de filtrado


#### Similitudes

**Paginación**

Aunque v2 tiene funciones de paginación adicionales (nuevos parámetros de paginación que te permiten navegar utilizando IDs de Post con `since_id` y `until_id`), tanto enterprise como v2 te permiten paginar usando tiempo (`fromDate` y `toDate` con enterprise, y `start_time` y `end_time` para v2).
 

**Zona horaria**

Como se mencionó en la sección de paginación, puedes navegar por diferentes páginas de datos usando tiempo tanto para enterprise como para v2. En ambos casos, usarás UTC como zona horaria al usar estos parámetros.

**Soporte para historial y metadatos de edición de Post**

Ambas versiones proporcionan metadatos que describen cualquier historial de edición. Consulta las [referencias de la API de search](/x-api/posts/recent-search) y la [página de fundamentos de edición de Post](/x-api/fundamentals/edit-posts) para obtener más detalles. 

#### Diferencias

**URLs de endpoint**

* Endpoints Enterprise:
    * 30 días - `http://gnip-api.x.com/search/30day/accounts/:account_name/:label.json`
    * Full-archive - `http://gnip-api.x.com/search/fullarchive/accounts/:account_name/:label.json`
* Endpoints X API v2
    * Recent (7 días) - `https://api.x.com/2/tweets/search/recent`
    * Full-archive - `https://api.x.com/2/tweets/search/all`

**Requisito de App y Project**

Los endpoints de X API v2 requieren que utilices credenciales de un Project al autenticar tus solicitudes. Todos los endpoints de X API v1.1 pueden usar credenciales de Apps o Apps asociadas con una App.
 

**Períodos de tiempo disponibles**

Ambos
