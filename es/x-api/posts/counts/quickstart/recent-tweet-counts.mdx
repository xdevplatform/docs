---
title: Recent Post Counts
sidebarTitle: Recent Post Counts
description: "Esta guía te muestra cómo obtener recuentos de Posts (volumen) de los últimos 7 días. Referencia del nivel estándar de X API v2 sobre quickstart."
keywords: ["quickstart de recent tweet counts", "quickstart de recent counts", "quickstart de tweet volume", "quickstart de counts", "recent search counts"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te muestra cómo obtener recuentos de Posts (volumen) de los últimos 7 días.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- El Bearer Token de tu App
</Note>

---

## Obtener recent Post counts

<Steps>
  <Step title="Construye una consulta">
    Usa la misma sintaxis de consulta que recent search. Por ejemplo, para contar Posts de @XDevelopers:

    ```
    from:XDevelopers
    ```
  </Step>

  <Step title="Realiza la solicitud">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/counts/recent?\
query=from%3AXDevelopers&\
granularity=day" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén recent Post counts
response = client.posts.count_recent(
    query="from:XDevelopers",
    granularity="day"
)

for bucket in response.data:
    print(f"{bucket.start}: {bucket.tweet_count} Posts")

print(f"Total: {response.meta.total_tweet_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén recent Post counts
const response = await client.posts.countRecent({
  query: "from:XDevelopers",
  granularity: "day",
});

response.data?.forEach((bucket) => {
  console.log(`${bucket.start}: ${bucket.tweet_count} Posts`);
});

console.log(`Total: ${response.meta?.total_tweet_count}`);
```

</CodeGroup>

  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": [
        {
          "end": "2024-01-16T00:00:00.000Z",
          "start": "2024-01-15T00:00:00.000Z",
          "tweet_count": 5
        },
        {
          "end": "2024-01-17T00:00:00.000Z",
          "start": "2024-01-16T00:00:00.000Z",
          "tweet_count": 3
        },
        {
          "end": "2024-01-18T00:00:00.000Z",
          "start": "2024-01-17T00:00:00.000Z",
          "tweet_count": 8
        }
      ],
      "meta": {
        "total_tweet_count": 16
      }
    }
    ```
  </Step>
</Steps>

---

## Opciones de granularidad

Controla cómo se agrupan los recuentos:

| Granularity | Description |
|:------------|:------------|
| `minute` | Recuentos por minuto |
| `hour` | Recuentos por hora (predeterminado) |
| `day` | Recuentos por día |

<CodeGroup dropdown>

```bash cURL
# Obtén recuentos por hora
curl "https://api.x.com/2/tweets/counts/recent?\
query=python%20lang%3Aen&\
granularity=hour" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén recuentos por hora
response = client.posts.count_recent(
    query="python lang:en",
    granularity="hour"
)

for bucket in response.data:
    print(f"{bucket.start}: {bucket.tweet_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén recuentos por hora
const response = await client.posts.countRecent({
  query: "python lang:en",
  granularity: "hour",
});

response.data?.forEach((bucket) => {
  console.log(`${bucket.start}: ${bucket.tweet_count}`);
});
```

</CodeGroup>

---

## Filtrar por rango de tiempo

Limita los recuentos a un período de tiempo específico:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/counts/recent?\
query=from%3AXDevelopers&\
start_time=2024-01-10T00%3A00%3A00Z&\
end_time=2024-01-15T00%3A00%3A00Z&\
granularity=day" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén los recuentos para un rango de tiempo específico
response = client.posts.count_recent(
    query="from:XDevelopers",
    start_time="2024-01-10T00:00:00Z",
    end_time="2024-01-15T00:00:00Z",
    granularity="day"
)

print(f"Total Posts: {response.meta.total_tweet_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén los recuentos para un rango de tiempo específico
const response = await client.posts.countRecent({
  query: "from:XDevelopers",
  startTime: "2024-01-10T00:00:00Z",
  endTime: "2024-01-15T00:00:00Z",
  granularity: "day",
});

console.log(`Total Posts: ${response.meta?.total_tweet_count}`);
```

</CodeGroup>

---

## Parámetros comunes

| Parameter | Description | Default |
|:----------|:------------|:--------|
| `query` | Consulta de búsqueda (obligatorio) | — |
| `granularity` | Tamaño del bucket de tiempo | `hour` |
| `start_time` | Timestamp más antiguo (ISO 8601) | Hace 7 días |
| `end_time` | Timestamp más reciente (ISO 8601) | Ahora |

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Full-archive counts" icon="vault" href="/x-api/posts/counts/quickstart/full-archive-tweet-counts">
    Obtén recuentos históricos de Posts
  </Card>
  <Card title="Construir una consulta" icon="/icons/xds/icon-search.svg" href="/x-api/posts/counts/integrate/build-a-query">
    Domina la sintaxis de consulta
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/posts/get-count-of-recent-posts">
    Documentación completa del endpoint
  </Card>
</CardGroup>
