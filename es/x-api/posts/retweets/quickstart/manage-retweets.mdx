---
title: Manage Retweets
sidebarTitle: Manage Retweets
description: "Esta guía te muestra cómo hacer Retweet y deshacer Retweets usando la X API. Referencia del nivel estándar de X API v2 sobre quickstart."
keywords: ["gestionar retweets", "retweetear posts", "unretweet", "quickstart de retweet", "tutorial de retweet", "guía de retweet"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te muestra cómo hacer Retweet y deshacer Retweets usando la X API.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- Un User Access Token (OAuth 1.0a o OAuth 2.0 PKCE)
</Note>

---

## Hacer Retweet a un Post

<Steps>
  <Step title="Obtén tu ID de usuario">
    Necesitas el ID de tu usuario autenticado. Puedes encontrarlo usando el [endpoint de user lookup](/x-api/users/lookup/introduction) o desde tu Access Token (la parte numérica es tu ID de usuario).
  </Step>

  <Step title="Obtén el ID del Post">
    Encuentra el ID del Post en la URL cuando visualizas un Post:

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── Este es el ID del Post
    ```
  </Step>

  <Step title="Envía la solicitud de Retweet">

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/users/123456789/retweets" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"tweet_id": "1228393702244134912"}'
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Retweet a un Post
response = client.posts.retweet(
    user_id="123456789",
    tweet_id="1228393702244134912"
)

print(f"Retweeted: {response.data.retweeted}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Retweet a un Post
const response = await client.posts.retweet("123456789", {
  tweetId: "1228393702244134912",
});

console.log(`Retweeted: ${response.data?.retweeted}`);
```

</CodeGroup>

  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": {
        "retweeted": true
      }
    }
    ```
  </Step>
</Steps>

---

## Deshacer un Retweet

Elimina un Retweet:

<CodeGroup dropdown>

```bash cURL
curl -X DELETE "https://api.x.com/2/users/123456789/retweets/1228393702244134912" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Deshacer un Retweet
response = client.posts.unretweet(
    user_id="123456789",
    tweet_id="1228393702244134912"
)

print(f"Retweeted: {response.data.retweeted}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Deshacer un Retweet
const response = await client.posts.unretweet("123456789", "1228393702244134912");

console.log(`Retweeted: ${response.data?.retweeted}`);
```

</CodeGroup>

**Respuesta:**

```json
{
  "data": {
    "retweeted": false
  }
}
```

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Consulta de retweets" icon="retweet" href="/x-api/posts/retweets/quickstart/retweets-lookup">
    Obtén los usuarios que retweetearon un Post
  </Card>
  <Card title="Quote Posts" icon="quote-left" href="/x-api/posts/quote-tweets/quickstart">
    Obtén los Quote Posts
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/users/repost-post">
    Documentación completa del endpoint
  </Card>
</CardGroup>
