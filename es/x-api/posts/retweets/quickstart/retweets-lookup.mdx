---
title: Retweets Lookup
sidebarTitle: Retweets Lookup
description: "Esta guía te muestra cómo recuperar los usuarios que retweetearon un Post específico. Referencia del nivel estándar de X API v2 sobre quickstart."
keywords: ["retweets lookup", "quickstart de retweets", "obtener retweets", "tutorial de retweets", "guía de retweets", "ejemplo de retweets"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te muestra cómo recuperar los usuarios que retweetearon un Post específico.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- El Bearer Token de tu App (para datos públicos) o un User Access Token (para métricas privadas)
</Note>

---

## Obtener los usuarios que retweetearon un Post

<Steps>
  <Step title="Encuentra el ID del Post">
    Obtén el ID del Post cuyos Retweets deseas consultar:

    ```
    https://x.com/XDevelopers/status/1354143047324299264
                                    └── Este es el ID del Post
    ```
  </Step>

  <Step title="Realiza la solicitud">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/1354143047324299264/retweeted_by?\
user.fields=created_at,username,verified" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python Python SDK
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén los usuarios que retweetearon un Post con paginación
for page in client.posts.get_retweeted_by(
    "1354143047324299264",
    user_fields=["created_at", "username", "verified"]
):
    for user in page.data:
        print(f"{user.username} - Joined: {user.created_at}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén los usuarios que retweetearon un Post con paginación
const paginator = client.posts.getRetweetedBy("1354143047324299264", {
  userFields: ["created_at", "username", "verified"],
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(`${user.username} - Joined: ${user.created_at}`);
  });
}
```

</CodeGroup>

  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": [
        {
          "created_at": "2008-12-04T18:51:57.000Z",
          "id": "17874544",
          "username": "TwitterSupport",
          "name": "Twitter Support",
          "verified": true
        },
        {
          "created_at": "2007-02-20T14:35:54.000Z",
          "id": "783214",
          "username": "Twitter",
          "name": "Twitter",
          "verified": true
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix"
      }
    }
    ```
  </Step>
</Steps>

---

## Incluir datos adicionales

Usa expansions para obtener datos relacionados, como los Posts fijados:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/1354143047324299264/retweeted_by?\
user.fields=created_at&\
expansions=pinned_tweet_id&\
tweet.fields=created_at" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtén los usuarios que retweetean con expansions
for page in client.posts.get_retweeted_by(
    "1354143047324299264",
    user_fields=["created_at"],
    expansions=["pinned_tweet_id"],
    tweet_fields=["created_at"]
):
    for user in page.data:
        print(f"{user.username}")
    # Los Posts fijados están en page.includes.tweets
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtén los usuarios que retweetean con expansions
const paginator = client.posts.getRetweetedBy("1354143047324299264", {
  userFields: ["created_at"],
  expansions: ["pinned_tweet_id"],
  tweetFields: ["created_at"],
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(user.username);
  });
  // Los Posts fijados están en page.includes?.tweets
}
```

</CodeGroup>

### Respuesta con expansion

```json title="Ejemplo de respuesta" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": [
    {
      "pinned_tweet_id": "1389270063807598594",
      "created_at": "2018-11-21T14:24:58.000Z",
      "id": "1065249714214457345",
      "username": "TwitterSpaces",
      "name": "Spaces"
    }
  ],
  "includes": {
    "tweets": [
      {
        "created_at": "2021-05-03T17:26:09.000Z",
        "id": "1389270063807598594",
        "text": "now, everyone with 600 or more followers can host a Space..."
      }
    ]
  }
}
```

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Gestionar Retweets" icon="retweet" href="/x-api/posts/retweets/quickstart/manage-retweets">
    Retweetea y deshaz Retweets
  </Card>
  <Card title="Quote Posts" icon="quote-left" href="/x-api/posts/quote-tweets/quickstart">
    Obtén los Quote Posts
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/posts/get-reposted-by">
    Documentación completa del endpoint
  </Card>
</CardGroup>
