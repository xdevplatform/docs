---
title: Guía de migración
sidebarTitle: Guía de migración
keywords: ["migración de hide replies", "migrar hide replies", "guía de migración", "migrar hide replies", "migración a v2"]
description: "El endpoint v2 de hide replies está reemplazando al endpoint de Labs de hide replies. Referencia del nivel estándar de X API v2 sobre hide replies."


---

import { Button } from '/snippets/button.mdx';

## Comparando los endpoints de hide replies de la X API

El endpoint v2 de hide replies está reemplazando al endpoint de Labs de hide replies. Si tienes código, apps o herramientas que utilizan la versión Labs de este endpoint y estás considerando migrar al endpoint más reciente de X API v2, esta guía es para ti.

Para usar la nueva X API v2 (incluido el endpoint de hide replies), deberás [optar por la nueva Developer Console](https://developer.x.com/en/portal/opt-in), crear un [Project](/resources/fundamentals/developer-apps) y añadir una App a ese Project. Luego puedes usar las credenciales asociadas con esa App para hacer solicitudes al endpoint de hide replies. Añadir la misma App que está inscrita para el endpoint de Labs v2 de hide replies mantendrá a tus usuarios autenticados.

La siguiente tabla compara las diferencias entre Labs y el endpoint más reciente de X API v2:
 

| **Description** | **Labs v2** | **X API v2** |
| :--- | :--- | :--- |
| Host domain | https://api.x.com | https://api.x.com |
| Endpoint path | /labs/2/tweets/:id/hidden | /2/tweets/:id/hidden |
| Autenticación | OAuth 1.0a User context | OAuth 1.0a User context |
| Métodos HTTP admitidos | PUT | PUT |
| Límites de tasa de solicitud predeterminados | 10 solicitudes por 15 minutos (compartidas entre todos los usuarios autenticados) | 50 solicitudes por 15 min (para cada usuario autenticado) |
| Puede ocultar respuestas | ✔︎  | ✔︎  |
| Puede mostrar una respuesta previamente ocultada | ✔︎  | ✔︎  |
| Puede ocultar o mostrar respuestas varias veces | ✔︎  | ✔︎  |
| Requiere el uso de credenciales de una [developer App](/resources/fundamentals/developer-apps) asociada a un [project](/resources/fundamentals/developer-apps) |     | ✔   |

---

## Ejemplos de código

### Ocultar una respuesta (v2)

<CodeGroup dropdown>

```bash cURL
curl -X PUT "https://api.x.com/2/tweets/1234567890/hidden" \
  -H "Authorization: OAuth ..." \
  -H "Content-Type: application/json" \
  -d '{"hidden": true}'
```

```python title="Python" lines wrap icon="python"
# Requiere autenticación OAuth 1.0a User Context
import requests
from requests_oauthlib import OAuth1

auth = OAuth1(
    "API_KEY", "API_SECRET",
    "ACCESS_TOKEN", "ACCESS_TOKEN_SECRET"
)

url = "https://api.x.com/2/tweets/1234567890/hidden"
response = requests.put(url, auth=auth, json={"hidden": True})
print(response.json())
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Oculta una respuesta
response = client.posts.hide_reply("1234567890", hidden=True)
print(f"Hidden: {response.data.hidden}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Oculta una respuesta
const response = await client.posts.hideReply("1234567890", { hidden: true });
console.log(`Hidden: ${response.data?.hidden}`);
```

</CodeGroup>

**Otros recursos de migración**

[Centro de migración de la X API](/x-api/migrate/overview)
