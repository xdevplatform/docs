---
title: Inicio rápido
sidebarTitle: Inicio rápido
description: "Esta guía te guía en la consulta de información de List usando los endpoints de List lookup. Referencia del nivel estándar de X API v2 sobre list lookup."
keywords: ["inicio rápido de list lookup", "tutorial de get list", "guía de list lookup"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te guía en la consulta de información de List usando los endpoints de List lookup.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- El Bearer Token de tu App
</Note>

---

## Obtener una List por ID

Recupera los detalles de una List específica:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/1234567890?\
list.fields=description,owner_id,member_count,follower_count,private,created_at" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtener una List por ID
response = client.lists.get(
    "1234567890",
    list_fields=["description", "owner_id", "member_count", "follower_count", "private", "created_at"]
)

print(f"List: {response.data.name}")
print(f"Members: {response.data.member_count}")
```

```javascript JavaScript SDK
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtener una List por ID
const response = await client.lists.get("1234567890", {
  listFields: ["description", "owner_id", "member_count", "follower_count", "private", "created_at"],
});

console.log(`List: ${response.data?.name}`);
console.log(`Members: ${response.data?.member_count}`);
```

</CodeGroup>

### Respuesta

```json title="Example response" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": {
    "id": "1234567890",
    "name": "Tech News",
    "description": "Top tech journalists and publications",
    "owner_id": "2244994945",
    "private": false,
    "member_count": 50,
    "follower_count": 1250,
    "created_at": "2023-01-15T10:00:00.000Z"
  }
}
```

---

## Obtener las Lists propiedad de un usuario

Recupera todas las Lists propiedad de un usuario específico:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/2244994945/owned_lists?\
list.fields=description,member_count,follower_count&\
max_results=100" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtener las Lists propiedad de un usuario con paginación
for page in client.lists.get_owned_lists(
    "2244994945",
    list_fields=["description", "member_count", "follower_count"],
    max_results=100
):
    for lst in page.data:
        print(f"{lst.name} - {lst.member_count} members")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtener las Lists propiedad de un usuario con paginación
const paginator = client.lists.getOwnedLists("2244994945", {
  listFields: ["description", "member_count", "follower_count"],
  maxResults: 100,
});

for await (const page of paginator) {
  page.data?.forEach((lst) => {
    console.log(`${lst.name} - ${lst.member_count} members`);
  });
}
```

</CodeGroup>

### Respuesta

```json title="Example response" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": [
    {
      "id": "1234567890",
      "name": "Tech News",
      "description": "Top tech journalists",
      "member_count": 50,
      "follower_count": 1250
    },
    {
      "id": "9876543210",
      "name": "Developer Tools",
      "description": "Useful tools for developers",
      "member_count": 25,
      "follower_count": 500
    }
  ],
  "meta": {
    "result_count": 2
  }
}
```

---

## Incluir información del propietario

Expande los datos de usuario del propietario:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/1234567890?\
list.fields=description,owner_id&\
expansions=owner_id&\
user.fields=username,verified" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtener List con información del propietario
response = client.lists.get(
    "1234567890",
    list_fields=["description", "owner_id"],
    expansions=["owner_id"],
    user_fields=["username", "verified"]
)

print(f"List: {response.data.name}")
# La información del propietario está en response.includes.users
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtener List con información del propietario
const response = await client.lists.get("1234567890", {
  listFields: ["description", "owner_id"],
  expansions: ["owner_id"],
  userFields: ["username", "verified"],
});

console.log(`List: ${response.data?.name}`);
// La información del propietario está en response.includes?.users
```

</CodeGroup>

### Respuesta con expansion

```json title="Example response" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": {
    "id": "1234567890",
    "name": "Tech News",
    "description": "Top tech journalists",
    "owner_id": "2244994945"
  },
  "includes": {
    "users": [
      {
        "id": "2244994945",
        "username": "XDevelopers",
        "verified": true
      }
    ]
  }
}
```

---

## Fields disponibles

| Field | Descripción |
|:------|:------------|
| `description` | Descripción de la List |
| `owner_id` | User ID del propietario |
| `private` | Si la List es privada |
| `member_count` | Número de miembros |
| `follower_count` | Número de seguidores |
| `created_at` | Fecha de creación de la List |

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="List Posts" icon="/icons/xds/icon-bulleted-list.svg" href="/x-api/lists/list-tweets/quickstart">
    Obtén Posts de una List
  </Card>
  <Card title="List members" icon="/icons/xds/icon-people.svg" href="/x-api/lists/list-members/quickstart/list-members-lookup">
    Obtén los miembros de una List
  </Card>
  <Card title="Gestionar Lists" icon="pen" href="/x-api/lists/manage-lists/quickstart">
    Crea y actualiza Lists
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/lists/list-lookup-by-list-id">
    Documentación completa del endpoint
  </Card>
</CardGroup>
