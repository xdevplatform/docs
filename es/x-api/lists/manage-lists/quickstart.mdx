---
title: Inicio rápido
sidebarTitle: Inicio rápido
description: "Esta guía te guía en la creación, actualización y eliminación de Lists. Referencia del nivel estándar de X API v2 sobre manage lists."
keywords: ["inicio rápido de manage lists", "tutorial de crear list", "guía de gestión de lists"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te guía en la creación, actualización y eliminación de Lists.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- User Access Token (OAuth 1.0a u OAuth 2.0 PKCE)
</Note>

---

## Crear una List

<Steps>
  <Step title="Prepara tu solicitud">
    Define el nombre de la List (obligatorio) y la descripción y ajustes de privacidad opcionales:

    ```json
    {
      "name": "Tech News",
      "description": "Top tech journalists and publications",
      "private": false
    }
    ```
  </Step>

  <Step title="Envía la solicitud">

<CodeGroup dropdown>

```bash cURL
curl -X POST "https://api.x.com/2/lists" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Tech News",
    "description": "Top tech journalists and publications",
    "private": false
  }'
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Crear una nueva List
response = client.lists.create(
    name="Tech News",
    description="Top tech journalists and publications",
    private=False
)

print(f"List created: {response.data.id} - {response.data.name}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Crear una nueva List
const response = await client.lists.create({
  name: "Tech News",
  description: "Top tech journalists and publications",
  private: false,
});

console.log(`List created: ${response.data?.id} - ${response.data?.name}`);
```

</CodeGroup>

  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": {
        "id": "1441162269824405510",
        "name": "Tech News"
      }
    }
    ```

    Guarda el `id` para actualizar o eliminar la List más adelante.
  </Step>
</Steps>

---

## Actualizar una List

Modifica el nombre, la descripción o la privacidad de una List:

<CodeGroup dropdown>

```bash cURL
curl -X PUT "https://api.x.com/2/lists/1441162269824405510" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Tech News & Insights",
    "description": "Updated description"
  }'
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Actualizar una List
response = client.lists.update(
    "1441162269824405510",
    name="Tech News & Insights",
    description="Updated description"
)

print(f"Updated: {response.data.updated}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Actualizar una List
const response = await client.lists.update("1441162269824405510", {
  name: "Tech News & Insights",
  description: "Updated description",
});

console.log(`Updated: ${response.data?.updated}`);
```

</CodeGroup>

**Respuesta:**

```json
{
  "data": {
    "updated": true
  }
}
```

---

## Eliminar una List

<Steps>
  <Step title="Obtén el ID de la List">
    Necesitas el ID de la List que quieres eliminar.
  </Step>

  <Step title="Envía la solicitud de eliminación">

<CodeGroup dropdown>

```bash cURL
curl -X DELETE "https://api.x.com/2/lists/1441162269824405510" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client
from xdk.oauth1_auth import OAuth1

oauth1 = OAuth1(
    api_key="YOUR_API_KEY",
    api_secret="YOUR_API_SECRET",
    access_token="YOUR_ACCESS_TOKEN",
    access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
)

client = Client(auth=oauth1)

# Eliminar una List
response = client.lists.delete("1441162269824405510")
print(f"Deleted: {response.data.deleted}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client, OAuth1 } from "@xdevplatform/xdk";

const oauth1 = new OAuth1({
  apiKey: "YOUR_API_KEY",
  apiSecret: "YOUR_API_SECRET",
  accessToken: "YOUR_ACCESS_TOKEN",
  accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
});

const client = new Client({ oauth1 });

// Eliminar una List
const response = await client.lists.delete("1441162269824405510");
console.log(`Deleted: ${response.data?.deleted}`);
```

</CodeGroup>

  </Step>

  <Step title="Confirma la eliminación">
    ```json
    {
      "data": {
        "deleted": true
      }
    }
    ```
  </Step>
</Steps>

<Warning>
Solo puedes eliminar Lists de tu propiedad.
</Warning>

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="List members" icon="/icons/xds/icon-people.svg" href="/x-api/lists/list-members/introduction">
    Añade y elimina List members
  </Card>
  <Card title="List lookup" icon="/icons/xds/icon-search.svg" href="/x-api/lists/list-lookup/quickstart">
    Recupera detalles de una List
  </Card>
  <Card title="Guía de integración" icon="/icons/xds/icon-book.svg" href="/x-api/lists/manage-lists/integrate">
    Conceptos clave y mejores prácticas
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/lists/create-list">
    Documentación completa del endpoint
  </Card>
</CardGroup>
