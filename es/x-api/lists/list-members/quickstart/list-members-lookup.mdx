---
title: List Members Lookup
sidebarTitle: List Members Lookup
description: "Inicio rápido para recuperar los miembros de una List de X con el endpoint List members lookup de X API v2, incluyendo autenticación y solicitudes de ejemplo."
keywords: ["inicio rápido de list members lookup", "list members lookup", "inicio rápido de get list members", "list membership lookup"]
---

import { Button } from '/snippets/button.mdx';

Esta guía te guía en la recuperación de los miembros de una List.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- El Bearer Token de tu App
</Note>

---

## Obtener miembros de la List

<Steps>
  <Step title="Encuentra el ID de la List">
    Puedes encontrar el ID de la List en la URL al ver una List:

    ```
    https://x.com/i/lists/84839422
                          └── Este es el ID de la List
    ```
  </Step>

  <Step title="Solicita los miembros de la List">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/84839422/members?\
user.fields=created_at,username,verified&\
max_results=100" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtener miembros de la List con paginación
for page in client.lists.get_members(
    "84839422",
    user_fields=["created_at", "username", "verified"],
    max_results=100
):
    for user in page.data:
        print(f"{user.username} - Joined: {user.created_at}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtener miembros de la List con paginación
const paginator = client.lists.getMembers("84839422", {
  userFields: ["created_at", "username", "verified"],
  maxResults: 100,
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(`${user.username} - Joined: ${user.created_at}`);
  });
}
```

</CodeGroup>

  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": [
        {
          "id": "1319036828964454402",
          "name": "Birdwatch",
          "username": "birdwatch",
          "created_at": "2020-10-21T22:04:47.000Z",
          "verified": true
        },
        {
          "id": "1065249714214457345",
          "name": "Spaces",
          "username": "TwitterSpaces",
          "created_at": "2018-11-21T14:24:58.000Z",
          "verified": true
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "5349804505549807616"
      }
    }
    ```
  </Step>
</Steps>

---

## Incluir datos adicionales

Usa expansions para obtener datos relacionados como Posts fijados:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/84839422/members?\
user.fields=created_at&\
expansions=pinned_tweet_id&\
tweet.fields=created_at" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtener miembros de la List con expansions
for page in client.lists.get_members(
    "84839422",
    user_fields=["created_at"],
    expansions=["pinned_tweet_id"],
    tweet_fields=["created_at"]
):
    for user in page.data:
        print(f"{user.username}")
    # Los Posts fijados están en page.includes.tweets
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtener miembros de la List con expansions
const paginator = client.lists.getMembers("84839422", {
  userFields: ["created_at"],
  expansions: ["pinned_tweet_id"],
  tweetFields: ["created_at"],
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(user.username);
  });
  // Los Posts fijados están en page.includes?.tweets
}
```

</CodeGroup>

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="Gestionar List members" icon="user-plus" href="/x-api/lists/list-members/quickstart/manage-list-members">
    Añade y elimina miembros
  </Card>
  <Card title="List lookup" icon="/icons/xds/icon-bulleted-list.svg" href="/x-api/lists/list-lookup/quickstart">
    Obtén detalles de una List
  </Card>
  <Card title="Referencia de la API" icon="/icons/xds/icon-code.svg" href="/x-api/lists/get-list-members">
    Documentación completa del endpoint
  </Card>
</CardGroup>
