---
title: Resumen de List Members
sidebarTitle: Resumen
description: "Los endpoints de List members te permiten consultar los miembros de una List y gestionar la List membership. Referencia del nivel estándar de X API v2 sobre inicio rápido."
keywords: ["inicio rápido de list members", "resumen de list members", "tutorial de list members", "inicio rápido de list membership"]
---

import { Button } from '/snippets/button.mdx';

Los endpoints de List members te permiten consultar los miembros de una List y gestionar la List membership.

<Note>
**Requisitos previos**

Antes de comenzar, necesitarás:
- Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
- El Bearer Token de tu App (para lookups)
- User Access Token (para gestionar miembros)
</Note>

---

## Endpoints disponibles

<CardGroup cols={2}>
  <Card title="List members lookup" icon="/icons/xds/icon-people.svg" href="/x-api/lists/list-members/quickstart/list-members-lookup">
    Obtén los miembros de una List
  </Card>
  <Card title="Gestionar List members" icon="user-plus" href="/x-api/lists/list-members/quickstart/manage-list-members">
    Añade y elimina miembros
  </Card>
</CardGroup>

---

## Autenticación

| Operación | Autenticación |
|:----------|:---------------|
| Consultar miembros | Bearer Token, OAuth 1.0a u OAuth 2.0 PKCE |
| Añadir/eliminar miembros | OAuth 1.0a u OAuth 2.0 PKCE |

---

## Ejemplo rápido

<CodeGroup dropdown>

```bash cURL
# Obtener miembros de la List
curl "https://api.x.com/2/lists/84839422/members" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python Python SDK
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obtener miembros de la List
for page in client.lists.get_members("84839422"):
    for user in page.data:
        print(f"{user.username}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obtener miembros de la List
const paginator = client.lists.getMembers("84839422");

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(user.username);
  });
}
```

</CodeGroup>

---

## Próximos pasos

<CardGroup cols={2}>
  <Card title="List lookup" icon="/icons/xds/icon-bulleted-list.svg" href="/x-api/lists/list-lookup/quickstart">
    Obtén detalles de una List
  </Card>
  <Card title="List Posts" icon="/icons/xds/icon-chat.svg" href="/x-api/lists/list-tweets/quickstart">
    Obtén Posts de una List
  </Card>
</CardGroup>
