---
title: "ClientConfig"
sidebarTitle: "ClientConfig"
description: "X API SDK の ClientConfig TypeScript インターフェースのリファレンス。X API クライアントの構成オプション。認証用の Bearer Token、OAuth1。"
---

X API クライアントの構成オプション

## プロパティ

<ResponseField name="baseUrl" type="string">
API リクエストのベース URL

</ResponseField>

<ResponseField name="bearerToken" type="string">
認証用の Bearer Token

</ResponseField>

<ResponseField name="accessToken" type="string">
OAuth 2.0 アクセストークン

</ResponseField>

<ResponseField name="oauth1" type="any">
認証用の OAuth1 インスタンス

</ResponseField>

<ResponseField name="headers" type="Record&lt;string, string&gt;">
リクエストに含めるカスタムヘッダー

</ResponseField>

<ResponseField name="timeout" type="number">
リクエストのタイムアウト（ミリ秒）

</ResponseField>

<ResponseField name="retry" type="boolean">
失敗したリクエストを自動的にリトライするかどうか

</ResponseField>

<ResponseField name="maxRetries" type="number">
リトライの最大試行回数

</ResponseField>
