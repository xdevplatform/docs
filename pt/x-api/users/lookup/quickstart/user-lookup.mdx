---
title: Guia rápido de consulta de usuário
sidebarTitle: Consulta de usuário
description: "Este guia mostra como consultar usuários pelo ID ou pelo username. Referência para o nível standard da X API v2 sobre quickstart."
keywords: ["guia rápido de consulta de usuário", "tutorial obter usuário", "guia usuário por ID", "usuário por username"]
---

import { Button } from '/snippets/button.mdx';

Este guia mostra como consultar usuários pelo ID ou pelo username.

<Note>
**Pré-requisitos**

Antes de começar, você precisará de:
- Uma [conta de desenvolvedor](https://developer.x.com/en/portal/petition/essential/basic-info) com um App aprovado
- O Bearer Token do seu App
</Note>

---

## Consultar por ID

### Um único usuário

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/2244994945?\
user.fields=created_at,description,verified,public_metrics" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter usuário por ID
response = client.users.get(
    "2244994945",
    user_fields=["created_at", "description", "verified", "public_metrics"]
)

print(f"Name: {response.data.name}")
print(f"Username: {response.data.username}")
print(f"Followers: {response.data.public_metrics.followers_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter usuário por ID
const response = await client.users.get("2244994945", {
  userFields: ["created_at", "description", "verified", "public_metrics"],
});

console.log(`Name: ${response.data?.name}`);
console.log(`Username: ${response.data?.username}`);
console.log(`Followers: ${response.data?.public_metrics?.followers_count}`);
```

</CodeGroup>

### Resposta

```json title="Exemplo de resposta" lines wrap icon="/icons/xds/icon-brackets.svg"
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "XDevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "The voice of the X developer community",
    "verified": true,
    "public_metrics": {
      "followers_count": 583423,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

### Vários usuários

Consulte até 100 usuários de uma vez:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users?\
ids=2244994945,783214,6253282&\
user.fields=username,verified" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter vários usuários por ID
response = client.users.get_users(
    ids=["2244994945", "783214", "6253282"],
    user_fields=["username", "verified"]
)

for user in response.data:
    print(f"{user.username} - Verified: {user.verified}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter vários usuários por ID
const response = await client.users.getUsers({
  ids: ["2244994945", "783214", "6253282"],
  userFields: ["username", "verified"],
});

response.data?.forEach((user) => {
  console.log(`${user.username} - Verified: ${user.verified}`);
});
```

</CodeGroup>

---

## Consultar por username

### Um único usuário

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/by/username/XDevelopers?\
user.fields=created_at,description,verified" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter usuário por username
response = client.users.get_by_username(
    "XDevelopers",
    user_fields=["created_at", "description", "verified"]
)

print(f"ID: {response.data.id}")
print(f"Name: {response.data.name}")
```

```javascript JavaScript SDK
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter usuário por username
const response = await client.users.getByUsername("XDevelopers", {
  userFields: ["created_at", "description", "verified"],
});

console.log(`ID: ${response.data?.id}`);
console.log(`Name: ${response.data?.name}`);
```

</CodeGroup>

### Vários usuários

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/users/by?\
usernames=XDevelopers,X,elonmusk&\
user.fields=created_at,verified" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter vários usuários por username
response = client.users.get_users_by_usernames(
    usernames=["XDevelopers", "X", "elonmusk"],
    user_fields=["created_at", "verified"]
)

for user in response.data:
    print(f"{user.username} - {user.created_at}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter vários usuários por username
const response = await client.users.getUsersByUsernames({
  usernames: ["XDevelopers", "X", "elonmusk"],
  userFields: ["created_at", "verified"],
});

response.data?.forEach((user) => {
  console.log(`${user.username} - ${user.created_at}`);
});
```

</CodeGroup>

---

## Fields disponíveis

| Field | Descrição |
|:------|:------------|
| `created_at` | Data de criação da conta |
| `description` | Bio do usuário |
| `profile_image_url` | URL do avatar |
| `verified` | Status de verificação |
| `public_metrics` | Contagens de seguidores/seguindo |
| `location` | Localização definida pelo usuário |
| `url` | Site do usuário |
| `protected` | Status de conta protegida |
| `pinned_tweet_id` | ID do Post fixado |

---

## Tratamento de erros

### Usuário não encontrado

```json
{
  "errors": [
    {
      "resource_type": "user",
      "title": "Not Found Error",
      "detail": "Could not find user with username: [nonexistent_user]."
    }
  ]
}
```

### Usuário protegido

Os dados de usuários protegidos ainda são retornados, mas você não conseguirá acessar os Posts deles a menos que os siga.

---

## Próximos passos

<CardGroup cols={2}>
  <Card title="Usuário autenticado" icon="user-check" href="/x-api/users/lookup/quickstart/authenticated-lookup">
    Obtenha o usuário atual
  </Card>
  <Card title="Guia de integração" icon="/icons/xds/icon-book.svg" href="/x-api/users/lookup/integrate">
    Conceitos-chave e melhores práticas
  </Card>
  <Card title="Referência da API" icon="/icons/xds/icon-code.svg" href="/x-api/users/get-user-by-id">
    Documentação completa do endpoint
  </Card>
</CardGroup>
