---
title: Quickstart
sidebarTitle: Quickstart
description: "Este guia mostra como recuperar Quote Posts (Posts que citam outro Post). Referência para o tier standard da X API v2 sobre quote tweets."
keywords: ["quickstart de quote tweets", "tutorial de quote tweets", "guia obter quotes"]
---

import { Button } from '/snippets/button.mdx';

Este guia mostra como recuperar Quote Posts (Posts que citam outro Post).

<Note>
**Pré-requisitos**

Antes de começar, você precisará de:
- Uma [conta de desenvolvedor](https://developer.x.com/en/portal/petition/essential/basic-info) com um App aprovado
- O Bearer Token do seu App
</Note>

---

<Steps>
  <Step title="Encontre o ID do Post" icon="/icons/xds/icon-chat.svg">
    Obtenha o ID do Post para o qual você quer encontrar quotes. Você pode encontrá-lo na URL do Post:

    ```
    https://x.com/XDevelopers/status/1409931481552543749
                                    └── Este é o ID do Post
    ```
  </Step>

  <Step title="Solicite Quote Posts" icon="terminal">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/tweets/1409931481552543749/quote_tweets?\
tweet.fields=created_at,public_metrics,author_id&\
expansions=author_id&\
user.fields=username,verified&\
max_results=10" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter Quote Posts com paginação
for page in client.posts.get_quote_tweets(
    "1409931481552543749",
    tweet_fields=["created_at", "public_metrics", "author_id"],
    expansions=["author_id"],
    user_fields=["username", "verified"],
    max_results=10
):
    for post in page.data:
        print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter Quote Posts com paginação
const paginator = client.posts.getQuoteTweets("1409931481552543749", {
  tweetFields: ["created_at", "public_metrics", "author_id"],
  expansions: ["author_id"],
  userFields: ["username", "verified"],
  maxResults: 10,
});

for await (const page of paginator) {
  page.data?.forEach((post) => {
    console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
  });
}
```

</CodeGroup>

  </Step>

  <Step title="Revise a resposta" icon="eye">
    ```json
    {
      "data": [
        {
          "id": "1495979553889697792",
          "text": "Great thread on the new API features! https://t.co/...",
          "author_id": "29757971",
          "created_at": "2022-02-22T04:31:34.000Z",
          "public_metrics": {
            "retweet_count": 5,
            "reply_count": 2,
            "like_count": 42,
            "quote_count": 1
          },
          "edit_history_tweet_ids": ["1495979553889697792"]
        }
      ],
      "includes": {
        "users": [
          {
            "id": "29757971",
            "username": "developer",
            "verified": false
          }
        ]
      },
      "meta": {
        "result_count": 1,
        "next_token": "avdjwk0udyx6"
      }
    }
    ```
  </Step>

  <Step title="Paginar os resultados" icon="/icons/xds/icon-arrow-right.svg">
    Os SDKs lidam com a paginação automaticamente. Para cURL, use o `next_token` para obter mais Quote Posts:

    ```bash
    curl "https://api.x.com/2/tweets/1409931481552543749/quote_tweets?\
    max_results=10&\
    pagination_token=avdjwk0udyx6" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Step>
</Steps>

---

## Próximos passos

<CardGroup cols={2}>
  <Card title="Retweets" icon="retweet" href="/x-api/posts/retweets/introduction">
    Consulte Retweets
  </Card>
  <Card title="Post lookup" icon="/icons/xds/icon-search.svg" href="/x-api/posts/lookup/introduction">
    Consulte Posts por ID
  </Card>
  <Card title="Referência da API" icon="/icons/xds/icon-code.svg" href="/x-api/posts/get-quoted-posts">
    Documentação completa do endpoint
  </Card>
</CardGroup>
