---
title: Consulta de membros da List
sidebarTitle: Consulta de membros da List
description: "Guia rápido para recuperar os membros de uma List do X com o endpoint de consulta de membros da List da X API v2, incluindo autenticação e exemplos de requisição."
keywords: ["guia rápido de consulta de membros da list", "consulta de membros da list", "guia rápido para obter membros da list", "consulta de list membership"]
---

import { Button } from '/snippets/button.mdx';

Este guia mostra como recuperar os membros de uma List.

<Note>
**Pré-requisitos**

Antes de começar, você precisará de:
- Uma [conta de desenvolvedor](https://developer.x.com/en/portal/petition/essential/basic-info) com um App aprovado
- O Bearer Token do seu App
</Note>

---

## Obter membros da List

<Steps>
  <Step title="Encontre o ID da List">
    Você pode encontrar o ID da List na URL ao visualizar uma List:

    ```
    https://x.com/i/lists/84839422
                          └── Este é o ID da List
    ```
  </Step>

  <Step title="Solicite os membros da List">

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/84839422/members?\
user.fields=created_at,username,verified&\
max_results=100" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter membros da List com paginação
for page in client.lists.get_members(
    "84839422",
    user_fields=["created_at", "username", "verified"],
    max_results=100
):
    for user in page.data:
        print(f"{user.username} - Joined: {user.created_at}")
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter membros da List com paginação
const paginator = client.lists.getMembers("84839422", {
  userFields: ["created_at", "username", "verified"],
  maxResults: 100,
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(`${user.username} - Joined: ${user.created_at}`);
  });
}
```

</CodeGroup>

  </Step>

  <Step title="Revise a resposta">
    ```json
    {
      "data": [
        {
          "id": "1319036828964454402",
          "name": "Birdwatch",
          "username": "birdwatch",
          "created_at": "2020-10-21T22:04:47.000Z",
          "verified": true
        },
        {
          "id": "1065249714214457345",
          "name": "Spaces",
          "username": "TwitterSpaces",
          "created_at": "2018-11-21T14:24:58.000Z",
          "verified": true
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "5349804505549807616"
      }
    }
    ```
  </Step>
</Steps>

---

## Incluir dados adicionais

Use expansions para obter dados relacionados, como Posts fixados:

<CodeGroup dropdown>

```bash cURL
curl "https://api.x.com/2/lists/84839422/members?\
user.fields=created_at&\
expansions=pinned_tweet_id&\
tweet.fields=created_at" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

```python title="Python SDK" lines wrap icon="python"
from xdk import Client

client = Client(bearer_token="YOUR_BEARER_TOKEN")

# Obter membros da List com expansions
for page in client.lists.get_members(
    "84839422",
    user_fields=["created_at"],
    expansions=["pinned_tweet_id"],
    tweet_fields=["created_at"]
):
    for user in page.data:
        print(f"{user.username}")
    # Posts fixados estão em page.includes.tweets
```

```javascript title="JavaScript SDK" lines wrap icon="square-js"
import { Client } from "@xdevplatform/xdk";

const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

// Obter membros da List com expansions
const paginator = client.lists.getMembers("84839422", {
  userFields: ["created_at"],
  expansions: ["pinned_tweet_id"],
  tweetFields: ["created_at"],
});

for await (const page of paginator) {
  page.data?.forEach((user) => {
    console.log(user.username);
  });
  // Posts fixados estão em page.includes?.tweets
}
```

</CodeGroup>

---

## Próximos passos

<CardGroup cols={2}>
  <Card title="Gerenciar membros da List" icon="user-plus" href="/x-api/lists/list-members/quickstart/manage-list-members">
    Adicione e remova membros
  </Card>
  <Card title="Consulta de List" icon="/icons/xds/icon-bulleted-list.svg" href="/x-api/lists/list-lookup/quickstart">
    Obtenha detalhes da List
  </Card>
  <Card title="Referência da API" icon="/icons/xds/icon-code.svg" href="/x-api/lists/get-list-members">
    Documentação completa do endpoint
  </Card>
</CardGroup>
