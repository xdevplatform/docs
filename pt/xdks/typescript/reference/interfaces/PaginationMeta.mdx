---
title: "PaginationMeta"
sidebarTitle: "PaginationMeta"
description: "Referência para PaginationMeta no X API SDK — interface TypeScript. Definições de campos, tipos e propriedades para payloads de requisição ou resposta da X API."
---

Metadados de paginação

## Propriedades

<ResponseField name="nextToken" type="string">
Token da próxima página

</ResponseField>

<ResponseField name="previousToken" type="string">
Token da página anterior

</ResponseField>

<ResponseField name="totalCount" type="number">
Contagem total

</ResponseField>

<ResponseField name="resultCount" type="number">
Contagem de resultados

</ResponseField>
