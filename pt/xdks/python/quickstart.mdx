---
title: Início rápido
sidebarTitle: Início rápido

description: "Este exemplo mostra como pesquisar rapidamente Posts usando o XDK com autenticação por Bearer Token. 1. Faça login no Console do desenvolvedor da X. 2."
---
Este exemplo mostra como pesquisar rapidamente Posts usando o XDK com autenticação por Bearer Token.
## Passo 1: Instale o SDK
```bash
pip install xdk
```
## Passo 2: Obtenha seu Bearer Token
1. Faça login no [Console do desenvolvedor da X](https://developer.x.com/en/portal/dashboard).
2. Crie ou selecione um app.
3. Em "Keys and Tokens", gere um Bearer Token (autenticação apenas de app).
## Passo 3: Escreva e execute seu primeiro script
Crie um arquivo `quickstart.py`:
```python title="Example" lines wrap icon="python"
# Import the client
from xdk import Client
# Replace with your actual Bearer Token
client = Client(bearer_token="YOUR_BEARER_TOKEN_HERE")
# Fetch recent Posts mentioning "api"
# search_recent returns an Iterator, so iterate over it
for page in client.posts.search_recent(query="api", max_results=10):
    if page.data and len(page.data) > 0:
        # Access first Post - Pydantic models support both attribute and dict access
        first_post = page.data[0]
        post_text = first_post.text if hasattr(first_post, 'text') else first_post.get('text', '')
        print(f"Latest Post: {post_text}")
        break
    else:
        print("No Posts found.")
        break
```
Execute:
```bash
python quickstart.py
```
**Saída esperada**:
```
Latest Post: Exciting updates on XDK Python SDK!
```
**Solução de problemas**: Se receber um erro 401, verifique novamente seu Bearer Token. Para limites de taxa (429), aguarde e tente novamente.
## Próximos passos
- Explore [Autenticação](/xdks/python/authentication) para entender como usar autenticação por Bearer Token (apenas de app), OAuth 2.0 com PKCE (contexto de usuário) e OAuth 1.0a (contexto de usuário legado).
- Saiba mais sobre [Paginação](/xdks/python/pagination) para casos de uso em que você deseja um grande número de resultados retornados sem se preocupar em fazer múltiplas chamadas de API.
- Aprofunde-se em [Streaming](/xdks/python/streaming) para aprender a trabalhar com dados em tempo real.
Para exemplos de código detalhados usando o XDK Python, confira nosso [repositório de exemplos de código no GitHub](https://github.com/xdevplatform/samples/tree/main/python).
