You are translating documentation files from English (en) into {{LANG_NAME}} ({{LANG_CODE}}).

Workspace root: ~/workspace
Reference (read-only) repo with original English files: /home/daytona/repos/xdevplatform/docs

## Task
For each source file listed below, read the English source, produce a {{LANG_NAME}} translation, and write it to the same path prefixed with the language code directory: `{{LANG_CODE}}/<same-path>`.

Example: source `x-api/posts/create-post.mdx` → write `{{LANG_CODE}}/x-api/posts/create-post.mdx`.

## Group: {{GROUP_LABEL}}

## Files to translate
{{FILES}}

## Translation rules — follow strictly

1. Preserve EXACTLY, unchanged:
   - YAML frontmatter keys. Translate only the VALUES of `title`, `sidebarTitle`, `description`, `keywords`. Do NOT translate `openapi:`, `icon:`, `mode:` values, or slugs/URLs/file paths.
   - All MDX components (`<Card>`, `<CardGroup>`, `<Note>`, `<Warning>`, `<Tip>`, `<Info>`, `<ResponseField>`, `<Button>`, `<Frame>`, `<Steps>`, `<Step>`, `<Tabs>`, `<Tab>`, `<Accordion>`, `<AccordionGroup>`, `<CodeGroup>`, etc.), their prop names, and prop values that are not human prose (`href`, `icon`, `type`, `name`, `required`, `cols`).
   - Code blocks (fenced with triple backticks) — do NOT translate code. You MAY translate human-language code comments inside them.
   - Inline code (backticks) — do NOT translate.
   - All URLs, relative paths, anchors (`/x-api/posts/create-post`, `https://...`).
   - Import statements (`import { X } from '/snippets/x.mdx';`).
   - HTML tags and their attributes.
   - Table structure (pipes, dashes). Translate cell text only.

2. TRANSLATE:
   - Body paragraphs, headings, list items, table cell text, image alt text, blockquote text, callout text.
   - `title`, `sidebarTitle`, `description`, and `keywords` values in frontmatter.
   - `<Card title="...">` visible-label prop values.
   - Markdown link visible text (not the URL).
   - Code block titles like ```bash Title="..."``` (human-readable title only).

3. Use real quotation marks (`"` and `'`). NEVER HTML entities (`&quot;`, `&#39;`, `&amp;`).

4. Keep in English (proper nouns / product / protocol names): X, X API, OAuth, OAuth 2.0, Bearer Token, Enterprise, Basic, Pro, Free, JSON, HTTP, GET, POST, PUT, DELETE, cURL, Python, TypeScript, SDK, XDK, endpoint paths like `/2/tweets`, field names like `tweet.fields`.

5. Use sentence case for headings where the target language convention prefers it.

6. For OpenAPI stub files (frontmatter only, e.g. `openapi: get /2/foo`) with no prose, still create the target file — copy the content unchanged.

## Workflow per file
1. `read` the source (repo-relative path like `x-api/posts/create-post.mdx`).
2. Translate content per rules above.
3. `write` to `{{LANG_CODE}}/<same-path>`.
4. Continue to the next file.

Do NOT commit, push, open PRs, or run git. Only use `read` and `write`.

Process EVERY file in the list. Do not skip. When done, output a one-line summary: "Translated N files into {{LANG_CODE}}."
